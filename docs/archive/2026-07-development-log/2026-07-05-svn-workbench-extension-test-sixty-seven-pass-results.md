# SVN Workbench 扩展测试记录：67 PASS

日期：2026-07-05

阶段：开发 -> 测试

整体进度：约 59%

## 本轮验证目标

验证 AI 拆分队列套用守护接入后，队列项必须先经过提交计划预览，且只有 `计划可提交` 的队列项才能套用。

同时确认既有能力无回归：

- SVN 状态刷新
- Quick Diff BASE 内容读取
- 提交候选文件收集
- 生成物过滤
- 右键文件夹范围限制
- AI 提交文件筛选
- AI 拆分提交
- 提交计划预览
- 提交说明生成
- 团队提交规范
- 当前范围更新
- 冲突中心与 AI 冲突建议

## 执行命令

```powershell
npm.cmd run compile
npm.cmd run test:extension
npm.cmd audit
```

## 验证结果

- TypeScript 编译：通过。
- VS Code 扩展测试：67 PASS。
- npm 安全审计：0 vulnerabilities。

## 本轮新增测试

### guards commit split queue apply by preview status

验证拆分队列套用守护：

- `notPreviewed` 队列项不能套用。
- `ready` 队列项可以套用。
- `blocked` 队列项不能套用。
- 守护函数会返回明确的拒绝原因，便于页面给出用户提示。

## 当前完整测试覆盖

当前 67 个扩展测试覆盖：

- 扩展激活与命令注册。
- SVN 状态刷新。
- Quick Diff BASE 内容读取。
- 生成物识别与提交过滤。
- 右键文件夹范围和多选范围边界。
- 根目录与文件夹提交候选收集。
- AI 提交文件筛选请求、本地回退、越权和幻觉路径拦截。
- AI 筛选解释映射、未分析状态、AI 决策过滤和批量选择动作。
- 提交候选按模块、AI 决策和默认策略统计分组。
- 分组级可选路径安全规则。
- AI 拆分提交请求、本地建议、路径校验、提交计划预览、队列管理、队列预览状态和套用守护。
- AI Provider 预设、模型列表、场景模型路由。
- AI 团队规则推荐请求、本地推荐和模型结果归一化。
- 提交页面打开、提交计划预览、提交前远端更新检查。
- 提交说明模板、团队规范、仓库级团队配置。
- 团队规则可视化配置页的输入归一化、校验和保存策略。
- AI 提交说明生成、轻量 diff 摘要、模板补全。
- 当前范围更新预览。
- SVN 冲突解析、冲突中心、AI 冲突建议、标记解决预览。

## 结论

AI 拆分队列已经具备“先预览、再套用”的基本闭环。

后续可以在此基础上继续推进逐条提交执行流，让队列项从 `待处理` 进一步流转到 `待提交`、`提交中`、`已完成` 或 `失败待处理`。
