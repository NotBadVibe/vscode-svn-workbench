# SVN Workbench 扩展测试记录：72 PASS

日期：2026-07-05

阶段：开发 -> 测试

整体进度：约 64%

## 本轮验证目标

验证 AI 拆分队列状态筛选视图：

- 可按状态只看失败项。
- 可按状态只看已完成项。
- 状态筛选可与隐藏已完成叠加。
- 统计中的当前可见项数量准确。
- `hiddenCompleted` 只统计被隐藏的已完成项。

## 执行命令

```powershell
npm.cmd run compile
npm.cmd run test:extension
npm.cmd audit
```

## 验证结果

- TypeScript 编译：通过。
- VS Code 扩展测试：72 PASS。
- npm 安全审计：0 vulnerabilities。

## 本轮新增测试

### filters commit split queue by status view

验证队列状态视图过滤：

- `getVisibleCommitSplitQueueItems` 可以只返回 failed 项。
- 开启隐藏已完成后，completed 视图会显示为空。
- `summarizeCommitSplitQueue` 会同时保留全量状态统计和当前可见数量。
- `hiddenCompleted` 只统计因隐藏已完成而被隐藏的完成项。

## 当前完整测试覆盖

当前 72 个扩展测试覆盖：

- 扩展激活与命令注册。
- SVN 状态刷新。
- Quick Diff BASE 内容读取。
- 生成物识别与提交过滤。
- 右键文件夹范围和多选范围边界。
- 根目录与文件夹提交候选收集。
- AI 提交文件筛选请求、本地回退、越权和幻觉路径拦截。
- AI 筛选解释映射、未分析状态、AI 决策过滤和批量选择动作。
- 提交候选按模块、AI 决策和默认策略统计分组。
- 分组级可选路径安全规则。
- AI 拆分提交请求、本地建议、路径校验、提交计划预览、队列管理、队列预览状态、套用守护、提交状态流转、专用提交入口、提交后处理、失败重试、完成项清理和状态筛选。
- AI Provider 预设、模型列表、场景模型路由。
- AI 团队规则推荐请求、本地推荐和模型结果归一化。
- 提交页面打开、提交计划预览、提交前远端更新检查。
- 提交说明模板、团队规范、仓库级团队配置。
- 团队规则可视化配置页的输入归一化、校验和保存策略。
- AI 提交说明生成、轻量 diff 摘要、模板补全。
- 当前范围更新预览。
- SVN 冲突解析、冲突中心、AI 冲突建议、标记解决预览。

## 结论

AI 拆分队列已经具备基础状态视图。

当队列项变多时，用户可以快速切换到失败、待提交或已完成视图，处理效率更接近专业 SVN 客户端的变更列表体验。
