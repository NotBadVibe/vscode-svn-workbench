# SVN Workbench 扩展测试记录：69 PASS

日期：2026-07-05

阶段：开发 -> 测试

整体进度：约 61%

## 本轮验证目标

验证 AI 拆分队列新增 `提交此项` 后：

- 未预览队列项不能提交。
- `计划可提交` 队列项允许进入提交入口。
- `提交中` 队列项不能重复提交。
- `已完成` 队列项不能再次提交。
- 顶部 `确认提交` 和队列项 `提交此项` 共用同一个提交流。

## 执行命令

```powershell
npm.cmd run compile
npm.cmd run test:extension
npm.cmd audit
```

## 验证结果

- TypeScript 编译：通过。
- VS Code 扩展测试：69 PASS。
- npm 安全审计：0 vulnerabilities。

## 本轮新增测试

### guards commit split queue dedicated submit action

验证队列项专用提交入口守护：

- `notPreviewed` 队列项不能通过 `提交此项`。
- `ready` 队列项可以通过 `提交此项`。
- `submitting` 队列项不能重复提交。
- `completed` 队列项不能再次提交。

## 当前完整测试覆盖

当前 69 个扩展测试覆盖：

- 扩展激活与命令注册。
- SVN 状态刷新。
- Quick Diff BASE 内容读取。
- 生成物识别与提交过滤。
- 右键文件夹范围和多选范围边界。
- 根目录与文件夹提交候选收集。
- AI 提交文件筛选请求、本地回退、越权和幻觉路径拦截。
- AI 筛选解释映射、未分析状态、AI 决策过滤和批量选择动作。
- 提交候选按模块、AI 决策和默认策略统计分组。
- 分组级可选路径安全规则。
- AI 拆分提交请求、本地建议、路径校验、提交计划预览、队列管理、队列预览状态、套用守护、提交状态流转和专用提交入口。
- AI Provider 预设、模型列表、场景模型路由。
- AI 团队规则推荐请求、本地推荐和模型结果归一化。
- 提交页面打开、提交计划预览、提交前远端更新检查。
- 提交说明模板、团队规范、仓库级团队配置。
- 团队规则可视化配置页的输入归一化、校验和保存策略。
- AI 提交说明生成、轻量 diff 摘要、模板补全。
- 当前范围更新预览。
- SVN 冲突解析、冲突中心、AI 冲突建议、标记解决预览。

## 结论

AI 拆分队列已经具备更直接的逐条提交流。

用户现在可以从队列项上直接点击 `提交此项`，但真正执行 SVN 命令前仍会经过后端校验和 VS Code 二次确认。
