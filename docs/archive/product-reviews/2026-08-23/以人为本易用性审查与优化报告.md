# SVN Workbench 以人为本易用性审查与优化报告

> 文档身份：`independent-usability-audit`
>
> 状态：独立产品评审资料，不属于任何版本范围，不表示任何方案已经纳入规划、实现或通过验收。
>
> 审查日期：2026-08-23。
>
> 审查范围：当前 v0.0.16 已发布界面、v0.0.17/v0.0.18 规划、本地开发工作树中可见的 v0.0.17 协议改动、主要 Svelte 页面、命令与菜单配置、发布截图和当前测试门禁。
>
> 不包含：真实用户访谈结果、真实设备人工验收结论、v0.0.17/v0.0.18 已完成声明、任何 SVN 写操作或发布操作。
>
> 版本隔离：本文只提供独立审查与候选建议，不修改 v0.0.17、v0.0.18 的版本 README、manifest、catalog、范围或验收状态；任何采纳都需要另行评审。
>
> 当前主权威：产品边界见 [`../../../current/产品与功能基线.md`](../../../current/产品与功能基线.md)，交互约束见 [`../../../current/设计与交互基线.md`](../../../current/设计与交互基线.md)，实现事实见 [`../../../current/实现与代码映射.md`](../../../current/实现与代码映射.md)，验收门禁见 [`../../../current/测试与验收基线.md`](../../../current/测试与验收基线.md)。

## 0. 结论先行

SVN Workbench 当前的主要问题不是能力不足，而是**能力数量、页面密度和安全说明已经超过普通用户一次任务所能消化的范围**。

产品已经建立了相当完整的安全边界：范围不能扩大、写操作先预览再确认、AI 不直接执行、旧结果会失效、敏感信息有保护。这些是产品最有价值的底座，应当保留。真正影响“好不好用”的，是底座之上的任务组织仍偏向实现结构：Changes、Commit、Changelists、Understanding、Repository、Activity 等模块分别证明自己能做什么，却没有持续回答用户最关心的三个问题：

1. 我现在最应该做什么？
2. 这次会影响哪里、多少文件，是否安全？
3. 做完以后下一步是什么？

因此，v0.0.17 和 v0.0.18 不应继续以“再补几个入口、筛选项或解释组件”为主线，而应改成：

- **v0.0.17：把日常闭环真正做短**。围绕“查看修改 → 检查差异 → 更新 → 提交 → 冲突恢复”减少决策点，统一主操作，补齐范围信任和结果下一步。
- **v0.0.18：在简化后的流程上做理解与打磨**。再补首次引导、术语解释、History 分页、筛选预设、AI 进度和视觉一致性。

最重要的产品原则是：

> 默认界面只服务当前任务；高频动作一步可达；低频能力按需展开；安全来自准确范围与一次高质量确认，而不是连续多次确认。

## 1. 审查依据与事实边界

### 1.1 已读取的项目基线

本次审查在提出修改建议前完整阅读了：

- [`../../../../README.md`](../../../../README.md)
- [`../../../README.md`](../../../README.md)
- [`../../../current/README.md`](../../../current/README.md)
- [`../../../current/产品与功能基线.md`](../../../current/产品与功能基线.md)
- [`../../../current/实现与代码映射.md`](../../../current/实现与代码映射.md)
- [`../../../current/设计与交互基线.md`](../../../current/设计与交互基线.md)
- [`../../../current/测试与验收基线.md`](../../../current/测试与验收基线.md)
- [`../../../releases/v0.0.17/README.md`](../../../releases/v0.0.17/README.md)
- [`../../../releases/v0.0.18/README.md`](../../../releases/v0.0.18/README.md)

### 1.2 当前仓库事实

| 观察项               | 当前事实                                                                                                                            | 对报告的意义                                                        |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| 已发布基线           | 根目录 [`package.json`](../../../../package.json) 为 `0.0.16`；v0.0.16 有发布截图与证据                                                | 本报告以 v0.0.16 界面作为可核对现状，不把规划当成已实现             |
| v0.0.17/v0.0.18 状态 | 两份 README 与 manifest 仍标记为 planned/draft                                                                                      | 用户所说“正在开发”与文档状态存在偏差；报告按“规划 + 局部开发中”处理 |
| 本地开发迹象         | [`workbenchProtocol.ts`](../../../../src/protocol/workbenchProtocol.ts) 已出现独立 `update` 模块相关修改，但工作树未形成完整可验收实现 | Update 拆分方向可以保留，但不能据此声称 v0.0.17 已完成              |
| 用户可见入口规模     | `package.json` 当前贡献 33 个命令；Explorer 主菜单含 5 个直接动作与 4 个子菜单；v0.0.16 Repository 页横排 11 个任务                 | 发现性问题不是“入口太少”，而是入口层级和任务优先级不清              |
| 内部交互规模         | 当前协议可见 13 个模块、26 个任务、114 个 Webview action                                                                            | 产品已经进入“需要强信息架构”的阶段，继续平铺能力会放大认知成本      |
| 页面规模             | `CommitModule.svelte` 约 1449 行、`ConflictsModule.svelte` 约 1059 行、Changes 与 Changelists 均约 947 行                           | 体验细节难统一并非偶然；巨型页面也提高了后续打磨成本                |

以上数量用于说明复杂度，不等同于“数量越少越好”。真正目标是让用户在每个时刻只看到与当前任务有关的少量决定。

### 1.3 视觉证据

本报告主要核对了 v0.0.16 的以下发布截图：

- [Changes](../../../releases/v0.0.16/artifacts/2026-08-21T07-44-03-711Z-6d625e2a/pages/01-changes.png)
- [Commit](../../../releases/v0.0.16/artifacts/2026-08-21T07-44-03-711Z-6d625e2a/pages/03-commit.png)
- [History](../../../releases/v0.0.16/artifacts/2026-08-21T07-44-03-711Z-6d625e2a/pages/04-history.png)
- [Conflicts](../../../releases/v0.0.16/artifacts/2026-08-21T07-44-03-711Z-6d625e2a/pages/05-conflicts.png)
- [Changelists](../../../releases/v0.0.16/artifacts/2026-08-21T07-44-03-711Z-6d625e2a/pages/06-changelists.png)
- [Understanding](../../../releases/v0.0.16/artifacts/2026-08-21T07-44-03-711Z-6d625e2a/pages/07-understanding.png)
- [Repository Update](../../../releases/v0.0.16/artifacts/2026-08-21T07-44-03-711Z-6d625e2a/pages/10-repository-update.png)
- [AI Settings](../../../releases/v0.0.16/artifacts/2026-08-21T07-44-03-711Z-6d625e2a/pages/11-settings-ai.png)
- [Diagnostics](../../../releases/v0.0.16/artifacts/2026-08-21T07-44-03-711Z-6d625e2a/pages/13-diagnostics.png)
- [Changes 5000 files](../../../releases/v0.0.16/artifacts/2026-08-21T07-44-03-711Z-6d625e2a/pages/14-changes-5000-files.png)

这些截图能证明布局与信息层级，不能替代真实用户测试。后文凡是涉及“用户可能困惑”“认知负担较高”的结论，均属于基于认知走查的推断，需用第 9 节的用户任务验证。

### 1.4 目标用户与核心任务

产品应优先服务两类人：

- **日常使用者**：知道 Update、Commit、Revert、Resolve 的大致含义，希望比命令行更省心。
- **低频 SVN 使用者**：能写代码，但不记得 SVN 术语、状态与安全边界，希望产品告诉他“现在该做什么”。

他们每天真正需要的不是 13 个模块，而是以下任务：

| 频率 | 用户任务                              | 用户心中的完成标准                                 |
| ---- | ------------------------------------- | -------------------------------------------------- |
| 很高 | 看今天改了什么                        | 快速找到文件、看 Diff、知道哪些能提交              |
| 很高 | 安全提交一组修改                      | 选对文件、写好说明、确认范围、提交完成             |
| 高   | 更新工作副本                          | 知道有没有远端变化与冲突风险，更新后能继续处理     |
| 中   | 解决冲突                              | 清楚当前冲突、保存结果、验证、标记解决、继续下一个 |
| 中   | 查历史与责任                          | 确认已加载范围，快速找到 revision、文件和作者      |
| 低   | 分支、标签、重定位、合并、属性、Patch | 需要时找得到，默认不干扰日常工作                   |
| 可选 | AI 解读、建议说明、语义拆分           | 能帮忙但不阻塞主流程，来源和数据边界可信           |

## 2. 总体诊断：功能导向强，任务导向弱

当前架构经多次迭代后形成了可靠的领域边界，但界面仍暴露了较多“系统知道什么”和“系统能做什么”，没有把它们压缩成“用户现在需要决定什么”。典型表现有：

- Changes 同时展示共享草稿、筛选、选择摘要、文件列表、Changelist、所选提交、当前范围提交；
- Commit 同时承担选择策略、AI 选择、提交说明、AI 建议、外发说明、提交前检查、命令预览和最终确认；
- Conflicts 同时出现“AI 分析”“解释冲突意图”“本地建议”“块级采用”“保存草稿”“标记解决”；
- Repository 在同一横向导航中放入 Update、Cleanup、Browse、Properties、Branch、Tag、Switch、Relocate、Merge、Patch/Shelf、Release Notes；
- History 告知“最近 N 条”，但用户不容易确认这是全部历史、当前页还是加载上限。

这类界面在功能演示时显得完整，在日常使用时却会产生三种成本：

1. **寻找成本**：用户先要判断应该进入哪个模块。
2. **解释成本**：进入模块后还要理解多个相近动作的差别。
3. **确认成本**：即使已经核对范围，仍可能经历复选框、预览按钮和确认对话框的连续批准。

安全与便捷不是对立项。范围准确、风险可见、一次性 token 和执行前复验属于安全；同一个意思反复让用户确认，不会显著增加安全，只会训练用户快速点击。

## 3. 问题清单与改进建议

优先级说明：

- **P0**：进入 v0.0.17 候选前必须解决，否则“发现性与效率”主题不成立。
- **P1**：建议纳入 v0.0.17 后半或 v0.0.18。
- **P2**：需有真实使用数据后决定，不应抢占日常闭环资源。

### U-01 · 日常任务被拆散在多个模块中（P0）

**问题点**

用户从 Changes 查看文件后，需要在 Changes、Diff、Commit、Changelists、Understanding 之间理解和切换。虽然会话共享选择已降低数据丢失风险，但“数据没有丢”不等于“用户没有丢失上下文”。

**怎么改**

- 建立“日常快速路径”：Changes 是起点，单击文件使用 VS Code 原生 Diff；选好文件后只有一个主操作“检查并提交 N 个文件”。
- Commit 接收选择和草稿后，默认直接落到“提交说明 + 检查结果”，不再次展示一套同等重量的选择策略；“调整文件”作为返回或展开动作。
- Changelist 与 Understanding 从日常流程的并列主动作降为“整理修改”和“需要帮助”中的按需能力。
- 不新建大首页，不改变 `moduleId + taskId + operationScope` 安全边界；只缩短已有模块的交接。

**好处**

- 降低模块选择与重复核对成本。
- 让普通提交路径先完成，高级能力仍然可达。
- 保留现有会话状态总线和范围守卫，不需要重做安全模型。

**引用案例**

[GitHub Desktop 的提交与审阅流程](https://docs.github.com/en/desktop/making-changes-in-a-branch/committing-and-reviewing-changes-to-your-project-in-github-desktop)把文件选择、Diff 和提交说明保持在同一个持续上下文；[VS Code SCM API](https://code.visualstudio.com/api/extension-guides/scm-provider)则证明了文件状态、点击 Diff、多选命令可以留在用户熟悉的原生 SCM 表面。

### U-02 · Changes 存在相互竞争且语义不够直接的提交动作（P0）

**问题点**

v0.0.16 Changes 截图底部同时存在“检查并提交所选（0）”与“检查当前范围并提交（5）”。此时列表中包含冲突与 external，用户看到“已选 0”却又看到可以提交“5”，需要推理“当前范围”和“可提交候选”的差别。

**怎么改**

- 每页只保留一个主按钮。
- 未选择时主按钮写“选择要提交的文件”，不直接暗示会提交整个范围。
- 有选择时写“检查并提交 3 个文件”。
- 若当前范围内有阻止项，按钮旁只显示一句原因：“另有 1 个冲突未包含，需先处理”；提供“处理冲突”次级入口。
- “提交当前范围”放到下拉菜单，并改成准确数量：“选择 3 个可提交文件”；执行后仍停在选择阶段，不直接进入写操作。

**好处**

- 消除“0 个已选但可提交 5 个”的认知冲突。
- 主操作数量、payload 与用户可见选择保持一致。
- 降低误以为范围会被静默扩大的不信任感。

**引用案例**

[GitHub Desktop](https://github.com/desktop/desktop)以文件复选状态决定最终提交范围，并把按钮写成“Commit N files to BRANCH”；[TortoiseSVN 提交对话框](https://tortoisesvn.net/docs/release/TortoiseSVN_en/tsvn-dug-commit.html)也以勾选文件作为提交范围的直接表达。

### U-03 · 范围栏没有完成“危险操作前的一眼确认”（P0）

**问题点**

当前 ScopeBar 主要显示项目、任务和根路径 chip；文件数量、工作副本 revision、入口来源和完整范围需要通过其他区域或 Tooltip 才能拼出来。对于 SVN 写操作，这些信息不是装饰，而是信任基础。

**怎么改**

将 v0.0.18 的 U-07 范围栏收尾提前到 v0.0.17，固定显示：

- 项目与工作副本；
- 当前任务；
- 范围摘要，例如“`src/webview/` · 3 个候选文件”；
- 工作副本 revision，例如 `r42`；
- 入口来源，例如“来自 Explorer 多选”；
- 可展开的完整范围清单，支持键盘、复制和关闭后焦点返回。

对写操作只显示“最终候选数量”，对普通浏览显示“当前范围数量”，两者不得混用。

**好处**

- 用户不进入技术详情也能判断“在哪里、多少项、基于哪个 revision”。
- 过期、项目切换和 external 边界更容易被理解。
- 使安全确认更短，因为关键信息已经持续可见。

**引用案例**

[GitLens](https://github.com/gitkraken/vscode-gitlens)把 Inspect 定位为当前所选对象的上下文详情，并在同一视图中维持仓库/分支定位；本项目不照搬 Git 分支模型，只借鉴“上下文持续可见”。

### U-04 · 安全确认存在“重复批准”风险（P0）

**问题点**

Repository 高级写操作当前可能先要求勾选“我已核对命令、目标和影响”，再打开通用 Operation Intent Dialog，最后再确认一次。多重确认看起来更谨慎，实际容易形成“连续点通过”的习惯。

**怎么改**

- 同一写操作只保留一次高质量意向单确认。
- 意向单内同时展示动作、数量、范围、revision、可恢复性、命令和阻止项。
- 一般写操作不再增加前置复选框。
- 只有不可逆且对象难以恢复的极高风险操作，才使用额外的目标复述或文本输入，并且要说明为什么需要。
- 读操作、仅生成预览、打开 Diff、复制路径不弹确认。

**好处**

- 减少无效点击和“确认疲劳”。
- 用户把注意力集中在真正需要核对的内容上。
- 不削弱 token、scopeHash、candidateHash、revision 与执行前 Host 复验。

**引用案例**

[lazygit 的确认面板](https://github.com/jesseduffield/lazygit/blob/master/docs/keybindings/Keybindings_en.md)保持统一的 Enter 确认、Esc 取消和复制出口；其价值不在快捷键本身，而在所有危险动作共用一个稳定确认模型。

### U-05 · Update 虽计划拆分，但闭环仍需围绕“结果”设计（P0）

**问题点**

把 Update 从 Repository 拆成独立模块是正确方向，但如果只是把现有 UpdateTask 搬到新窗口，用户仍要经历“进入页面—重新检查—看数字—确认—自己寻找冲突页”。

**怎么改**

独立 Update 页只保留三个状态：

1. **未检查**：主操作“检查远端更新”。
2. **已检查**：显示本地修改、远端修改、重叠路径和风险；主操作“更新当前范围”。
3. **已完成**：显示新增/修改/删除/冲突数量；有冲突时主操作立即变为“处理 N 个冲突”，无冲突时变为“查看更新后的修改”或“返回编辑”。

进度阶段使用“连接仓库 → 检查远端 → 合并到工作副本 → 刷新状态”，允许取消，并明确取消后会重新采集状态。

**好处**

- Update 不再只是一个命令预览页，而是一个完成后有出口的任务。
- 冲突处理入口紧贴结果，不需要用户回忆模块名称。
- 与当前 v0.0.17 协议中 `UpdateSnapshot.conflicts` 的方向一致。

**引用案例**

[TortoiseSVN Update](https://tortoisesvn.net/docs/release/TortoiseSVN_en/tsvn-dug-update.html)在完成页汇总 updated/added/removed/conflicted 数量，并允许从冲突条目直接进入合并工具；这是 SVN 场景最直接的可复用案例。

### U-06 · 冲突页的帮助入口多于用户当前需要的决定（P0）

**问题点**

当前冲突页同时提供“AI 分析”“解释冲突意图”、块级“采用我的/对方/保留两者”、本地合并建议、草稿保存与 Resolve。用户需要先理解多个建议来源，再判断自己是否已经保存、是否可以标记解决。

**怎么改**

把页面固定成一条可见流程：

```text
选择冲突文件 → 处理冲突块 → 保存工作副本 → 运行/查看验证建议 → 标记解决 → 下一个冲突
```

- 顶部只显示进度：“剩余 1/2 个冲突”。
- 默认工作区聚焦当前冲突与合并结果；“我的修改/对方修改/BASE”作为辅助 Tab。
- “AI 分析”和“解释冲突意图”合并为一个“需要帮助”入口，展开后再选择“本地建议”或“模型解释”。
- Resolve 按钮前显示检查清单：冲突标记已清除、已保存、文件仍在范围内；无法自动验证的项目明确写“需人工确认”。
- Resolve 成功后自动选中下一个未解决冲突；全部完成后给“返回 Update 结果/查看本地修改”。

**好处**

- 用户始终知道自己在冲突流程的哪一步。
- AI 从主任务竞争者变成按需帮助。
- 减少“保存内容”和“标记解决”两个概念混淆。

**引用案例**

[TortoiseSVN 合并冲突说明](https://tortoisesvn.net/docs/release/TortoiseSVN_en/tsvn-dug-merge.html)明确区分 Edit、保存和 Mark as resolved，并只在满足条件后启用标记解决；[GitButler 冲突流程](https://github.com/gitbutlerapp/gitbutler/blob/master/crates/but/skill/references/reference.md)也把 resolve status、编辑和 finish 组织为连续步骤。二者都说明“进度顺序”比同时展示更多工具更重要。

### U-07 · AI 在多个页面占据与确定性任务同等的视觉权重（P0）

**问题点**

Commit、Changelists、Conflicts、Understanding 都有模型相关按钮、外发说明、建议来源和证据区。透明说明是必要的，但当 AI 与本地规则、提交、保存、Resolve 同时处于首屏，用户会把时间花在判断“要不要用 AI”，而不是完成 SVN 任务。

**怎么改**

- AI 统一放入“需要帮助”或“生成建议”区域，默认折叠；核心 SVN 动作不依赖展开。
- 本地规则自动运行并以内联状态呈现，不使用“智能”“AI”字样。
- 用户主动选择模型能力后，才展示模型、预算、外发范围与回执。
- 同一页面最多一个模型入口；例如冲突页不同时出现“AI 分析”和“解释冲突意图”。
- AI 建议采用后仍回到任务主流程，不让建议卡长期占据首屏。

**好处**

- AI 仍然可信、可控，但不会把产品变成模型配置器。
- 未配置模型时界面更干净，核心 SVN 流程更像完整产品而非降级模式。
- 降低用户对“是否必须把代码发给模型”的焦虑。

**引用案例**

[GitHub Desktop](https://docs.github.com/en/desktop/making-changes-in-a-branch/committing-and-reviewing-changes-to-your-project-in-github-desktop)把生成提交说明作为 Summary 附近的可选动作，手写与提交主流程始终存在；[GitButler Commit Editing](https://docs.gitbutler.com/features/branch-management/commits)也把生成信息放在开始提交后的编辑上下文中，而不是独立要求用户先进入 AI 模块。

### U-08 · Repository 把日常、维护和危险操作放在同一层级（P1）

**问题点**

v0.0.16 Repository 页面横排 11 个任务，Update 与 Branch/Tag/Relocate/Merge/Release Notes 同层。即使 v0.0.17 拆走 Update，其余任务仍缺少面向用户目的的结构。

**怎么改**

Repository 只保留三组：

- **浏览与属性**：浏览仓库、SVN 属性；默认展开。
- **分支与集成**：创建分支、创建标签、切换、合并；折叠，记忆用户最近使用。
- **维护与迁移**：Cleanup、Relocate、Patch/Shelf、发布说明；折叠。

Relocate 和其他高风险项放到“高级操作”子组，不参与最近任务推荐。页面首屏不再横排全部任务，而是显示分组与最近使用的 1～2 项。

**好处**

- 低频能力仍然完整，但不会干扰日常操作。
- 任务名称可以写得更清楚，不受横向宽度限制。
- 小屏和 200% 缩放更自然。

**引用案例**

[GitLens](https://github.com/gitkraken/vscode-gitlens/blob/main/README.md)把多个仓库视图收敛为同一 tabbed view，避免多个折叠标题争夺空间；[VS Code Views UX](https://code.visualstudio.com/api/ux-guidelines/views)明确建议限制 View 数量和工具栏动作，空态只保留主动作与少量链接。

### U-09 · History 没有清楚表达“已加载到哪里”（P1）

**问题点**

当前界面写“最近 N 条修订”，实现规划提到过去存在固定 100 条上限。用户无法区分“仓库只有这些”“搜索没有匹配”还是“更早记录尚未加载”。

**怎么改**

- 列表头显示“已加载最近 100 条 · r240–r141”。
- 列表底部提供“加载更早记录”，不是隐藏在设置中的 limit。
- 搜索分两层：本地过滤当前已加载结果；需要更远历史时提供“在仓库中继续搜索”。
- 支持 revision、作者、日期范围的服务端查询；长任务可取消。
- 选中 revision 后详情保持，不因加载更多跳回顶部。

**好处**

- 用户知道结果是否完整，不会把“没加载”误判为“没有”。
- 大仓库性能可控。
- 与当前双栏 History 结构兼容。

**引用案例**

[TortoiseSVN Revision Log](https://tortoisesvn.net/docs/release/TortoiseSVN_en/tsvn-dug-showlog.html)明确说明日志默认加载上限并允许继续获取；其三窗格“修订—说明—变更路径”也验证了当前双栏方向是合理的，问题主要在加载边界说明。

### U-10 · 空状态与异常恢复仍偏“说明系统状态”，不够任务化（P1）

**问题点**

v0.0.15 已让 Diagnostics 动作可执行，这是正确改进。但各模块空态仍可能只说明“暂无历史”“没有匹配”“没有冲突”，没有稳定给出下一步；非 SVN 文件夹、干净工作副本、筛选为空、尚未加载和加载失败之间也容易使用相似视觉。

**怎么改**

所有空态遵守三句话：

1. 发生了什么；
2. 这是否正常；
3. 现在可以做什么。

示例：

- 干净工作副本：“当前范围没有本地修改，这是正常状态。可以检查远端更新或查看历史。”
- 筛选为空：“5 个文件中没有匹配‘test’的结果。清除筛选或只看已选文件。”
- 历史未加载：“尚未请求更早记录。加载 r140 之前的修订。”
- 非 SVN 文件夹：“此文件夹不是 SVN 工作副本。打开已有工作副本，或检查 SVN CLI 路径。”

**好处**

- 新用户不需要通过错误页学习产品模块。
- 降低空态与故障状态混淆。
- 恢复动作可直接用于 E2E 验收。

**引用案例**

[VS Code Welcome Views 指南](https://code.visualstudio.com/api/ux-guidelines/views)建议空态内容短、按钮只用于主动作、链接文字直接说明目的；这正适合本项目的空态矩阵。

### U-11 · 快捷键能力较完整，但发现性不足（P1）

**问题点**

列表已支持 Ctrl/⌘+A、PageUp/PageDown、Shift+F10、Home/End、Escape 与 IME 保护，但普通用户很难从界面发现。只在文档中存在的效率能力，无法改善日常体验。

**怎么改**

- 列表聚焦后在底部显示一行上下文快捷提示，例如“Space 选择 · Enter 看差异 · Shift+F10 更多 · / 搜索”。
- 提供 `?` 打开当前区域帮助，内容从实际绑定生成或集中维护，避免文档漂移。
- 首次使用某个手势时只提示一次，可关闭，不使用全屏教程。
- 所有图标按钮在 hover 与键盘 focus 时都显示 Tooltip。

**好处**

- 提升熟练用户效率，同时不增加首屏永久控件。
- 键盘行为从“测试通过”变成“用户可发现”。

**引用案例**

[gitui](https://github.com/gitui-org/gitui)将“context based help”作为核心能力，帮助内容只显示当前面板可用动作；[lazygit 快捷键表](https://github.com/jesseduffield/lazygit/blob/master/docs/keybindings/Keybindings_en.md)也按 Files、Branches、Confirmation 等上下文分组，而不是提供一张无差别总表。

### U-12 · 视觉密度高，主次层级经常被边框和说明文字稀释（P1）

**问题点**

多个页面存在重复主标题、眉题、说明段、外发预览、卡片边框和 notice。Commit 首屏尤其明显：左侧选择工具条、规则、AI 按钮，右侧提交说明、外发说明、建议卡、Diff、检查区与最终按钮同时竞争注意力。

**怎么改**

- ScopeBar 已显示任务名时，页面内部不再重复同名 H1。
- 每个页面只保留一个强调色主按钮；次级按钮使用轻量样式；危险按钮只在危险步骤出现。
- 边框只用于独立滚动区、风险区和可折叠信息，不给每段文字套卡片。
- 长说明改成一行摘要 + “查看详情”；外发回执只在用户选择模型能力后出现。
- 统一 8/12/16/24px 间距层级和标题字号；状态 badge 不承担按钮职责。

**好处**

- 用户能先看到任务和主动作，再按需阅读说明。
- 720×480 与 200% 缩放更容易保持可达。
- 减少不同模块各自创造视觉语法。

**引用案例**

[VS Code Webview UX](https://code.visualstudio.com/api/ux-guidelines/webviews)强调 Webview 只承担原生 API 无法表达的复杂详情，并避免重复现有功能；本项目不需要移除 Svelte，而需要让 Webview 聚焦真正复杂的任务部分。

### U-13 · 巨型页面让“细节优化”难以持续（P1，工程支撑）

**问题点**

Commit、Conflicts、Changes、Changelists 和 Settings Selection 都已成为近千行或超过千行的组件。它们混合数据派生、快捷键、选择、回执、Dialog、风险提示和布局。任何细节改动都容易触发大范围回归，也导致相同交互在不同页面实现略有差异。

**怎么改**

- 不做无关重构，只在对应体验改动中提取领域组件。
- 优先提取：`TaskSummary`、`PrimaryActionBar`、`AssistancePanel`、`TaskEmptyState`、`ConflictStepBar`、`HistoryLoadBoundary`。
- 选择、范围、意向单和列表继续复用现有共享底座，不建立第二套算法。
- Host 纯逻辑继续进入对应领域目录，不扩大 `WorkbenchController.ts`。

**好处**

- 视觉和文案细节可以在一个位置统一修复。
- 降低 v0.0.17/v0.0.18 的回归风险。
- 组件测试更接近用户任务，而不是只检查某个按钮存在。

### U-14 · 当前验收擅长证明“正确”，不擅长证明“顺手”（P0）

**问题点**

现有门禁覆盖协议、安全、范围、IME、键盘、滚动、主题和真实 SVN，这是优势。但“可见、可点、无横向滚动”不能证明用户知道该点哪个按钮，也不能证明一次任务的决策成本合理。

**怎么改**

- 在 v0.0.17 候选前进行 5–7 名形成性走查：参与者“用过 SVN，但未使用过本产品”。
- 测试时不给模块名称提示，只给任务目标。
- 记录任务完成率、首次停顿点、返回次数、误选动作、完成时间和任务后信心评分。
- 自动化继续证明安全与状态正确；真人走查证明入口、文案和顺序可理解，两者不能互相替代。

**好处**

- 避免继续用更多说明弥补结构问题。
- 让“以人为本、便捷为本”成为可验收目标，而非主观口号。

## 4. 建议的目标信息架构

### 4.1 双速工作台

不建议再建大首页，也不建议把所有复杂能力强塞回原生 SCM。更适合本项目的是“双速工作台”：

```text
快速路径（高频、低解释成本）
Explorer / 编辑器 / 原生 SCM
  ├─ 看状态与 Diff
  ├─ 选择文件
  ├─ 打开 Changes / Update / Commit / Conflicts
  └─ 执行简单只读动作

深度任务（复杂、需要范围与证据）
Svelte 独立任务窗口
  ├─ 提交检查与最终确认
  ├─ Update 风险与结果
  ├─ 冲突编辑与 Resolve
  ├─ History 详情与恢复
  └─ Repository 高级操作、AI 回执与证据
```

这不是新建平行业务 UI：原生 SCM 已经存在，Svelte 仍是复杂业务界面的唯一实现。调整的是“什么任务应该在哪个表面完成”。

### 4.2 日常任务优先级

全局只按以下顺序提升动作：

1. 有冲突：处理冲突。
2. 工作副本需要恢复：进入 Cleanup/恢复。
3. 有可提交修改：查看/提交修改。
4. 工作副本干净：检查远端更新。
5. 没有阻塞：查看历史或继续编辑。

推荐必须高置信、可解释、可忽略，不持久惩罚用户。不要在每个模块永久显示一条“推荐下一步”大横幅；只有冲突、锁定、过期等强状态使用全宽 notice，普通推荐放在 ScopeBar 下的一行 Task Summary 或主按钮文案中。

### 4.3 标准页面骨架

每个任务只使用以下五层：

```text
ScopeBar：在哪里 / 什么范围 / 哪个 revision
Task Summary：当前状态 / 为什么 / 下一步
Task Body：完成当前任务所需的列表、Diff 或表单
Primary Action：唯一主动作 + 必要的次级动作
Result：发生了什么 / 下一步 / 恢复入口
```

设置、证据、命令、模型预算、完整路径等信息按需展开，不与主任务争夺首屏。

## 5. 各模块建议方案

### 5.1 Changes：从“综合控制台”改为“今天改了什么”

**保留**

- 搜索、状态筛选、排序、密度、键盘多选、路径详情、窗口化、external/blocked 标识。

**调整**

- 默认折叠共享提交草稿；只有已有草稿时显示一行摘要“已有提交草稿，继续编辑”。
- 顶部工具条收敛为搜索、状态、更多筛选；选择摘要仅在产生选择后出现。
- 双击或 Enter 打开原生 Diff；行尾最多保留 1 个常用动作，其余进入菜单。
- Sticky 底栏只保留“检查并提交 N 个文件”主按钮和“整理到变更集”次级动作。
- 冲突行直接提供“处理冲突”，不允许进入提交选择。
- 5000 文件时默认按目录分组或提供树/平铺切换；显示“已加载 5000，当前筛选 N”，避免仅靠长列表。

**预期结果**

用户进入后 3 秒内能回答：有几个可提交文件、有没有冲突、下一步按钮是什么。

### 5.2 Commit：从“所有选择与 AI 能力集合”改为“提交最后一公里”

**保留**

- 精确候选、草稿保护、团队规则、受限差异回执、建议不覆盖草稿、提交前远端检查、意向单。

**调整**

- 默认两段：上方紧凑的“3 个文件待提交”摘要，下方提交说明与检查结果。
- 文件选择默认折叠；点击“调整文件”才展开完整列表。
- “应用本地规则”自动完成并展示结果，不作为首屏按钮。
- “获取 AI 建议”改为提交说明旁的魔法棒次级动作；外发回执仅在点击后出现。
- 提交前检查自动在选择或草稿稳定 300–500ms 后执行；“重新检查”只在过期/失败时出现。
- 最终按钮始终写明仓库/项目语义和数量：“提交 3 个文件”。
- Operation Intent Dialog 只在真正执行前出现一次。

**预期结果**

从 Changes 带入选择后，用户只需要核对文件摘要、写说明、确认一次。

### 5.3 Update：独立、短、结果驱动

按 U-05 的三状态模型完成当前 v0.0.17 拆分。不要保留 Repository 式任务导航，不在 Update 页展示其他仓库高级任务。

### 5.4 Conflicts：按步骤推进，不让来源争夺注意力

按 U-06 重排。建议增加 `ConflictStepBar`，将“是否保存”“是否仍有冲突标记”“是否可 Resolve”做成可核对状态，而不是继续增加说明卡片。

### 5.5 Changelists：以“任务分组”而非“AI 功能”开场

- 左侧“未分组修改”，右侧“任务组”；选中文件后可拖放或点击“移动到…”。
- 默认本地按目录/类型建议只作为轻量“自动整理”入口。
- “按改动意图拆分（含差异需确认）”放入“更多整理方式”，主动选择后再展示 AI 回执。
- 组卡标题直接可编辑；应用前只确认实际变更的 Changelist 操作。

[TortoiseSVN Changelists](https://tortoisesvn.net/docs/release/TortoiseSVN_en/tsvn-dug-changelists.html)证明了在修改检查和提交表面直接分组、拖放、按组选择，比另开一个抽象“分组建议”流程更贴近日常心智；[GitButler Parallel Branches](https://docs.gitbutler.com/features/branch-management/virtual-branches)的 lane 与拖放可作为视觉参考，但不能照搬其 Git 虚拟分支语义。

### 5.6 History：明确加载边界，保留选择上下文

按 U-09 实施。搜索、比较和恢复属于同一任务，但“恢复”是写操作，必须保持当前准确预览与确认契约。

### 5.7 Repository：分组与渐进展开

按 U-08 实施。Update 拆走后，Repository 默认入口应是 Browse，而不是任一危险或维护任务。最近使用只能提升同组内入口，不得把 Relocate 等危险项自动提升为全局主动作。

### 5.8 Diagnostics 与首次使用：恢复优先于教学

- 启动失败时直接显示最可能的一个主动作，例如“选择 SVN 可执行文件”。
- 成功后不要求用户先学习所有模块，只给“查看本地修改”。
- v0.0.18 的三分钟引导必须复用简化后的真实页面，并在最终确认前结束。
- 引导可以跳过、重开，不用弹窗阻塞每次启动。

### 5.9 Settings 与 Activity：不进入日常主导航

- Settings 只从齿轮、错误恢复和相关功能入口打开。
- Activity 在操作完成通知和“更多”中可达；只有发生失败或存在恢复动作时提升入口。
- AI、团队规则、SVN 安全继续分 Tab，但首屏只解释当前状态和一个主动作，不重复大段产品说明。

## 6. 开源项目参考与适用边界

| 项目/文档                                                                                                                                                                                                 | 值得借鉴的设计                                                           | 适用于 SVN Workbench                                | 不应照搬                                                     |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | --------------------------------------------------- | ------------------------------------------------------------ |
| [GitHub Desktop](https://github.com/desktop/desktop) / [提交与审阅文档](https://docs.github.com/en/desktop/making-changes-in-a-branch/committing-and-reviewing-changes-to-your-project-in-github-desktop) | Changes、Diff、提交说明保持同一上下文；文件/行级选择；按钮显示数量和目标 | 精简 Changes→Commit 交接、唯一主操作、Diff 贴近文件 | Git staging、push、branch ruleset 语义不能直接映射到 SVN     |
| [TortoiseSVN 修改检查](https://tortoisesvn.net/docs/release/TortoiseSVN_en/tsvn-dug-wcstatus.html)                                                                                                        | 本地与远端状态在同一检查表面；可更新选中项                               | Update 预检、重叠风险、选中范围更新                 | Windows Shell 外观与颜色依赖不适合 VS Code，也不能只靠颜色   |
| [TortoiseSVN Changelists](https://tortoisesvn.net/docs/release/TortoiseSVN_en/tsvn-dug-changelists.html)                                                                                                  | 分组直接出现在修改检查和提交对话框；拖放与组级选择                       | Changelist 贴近日常修改列表、按组提交               | external 与 Changelist 分组冲突需继续遵守本项目仓库边界      |
| [TortoiseSVN Update](https://tortoisesvn.net/docs/release/TortoiseSVN_en/tsvn-dug-update.html)                                                                                                            | 完成页汇总变更与冲突，可从冲突进入合并                                   | Update 结果与 Conflicts 直达闭环                    | 不能只用颜色表达状态                                         |
| [VS Code SCM API](https://code.visualstudio.com/api/extension-guides/scm-provider) / [source-control sample](https://github.com/microsoft/vscode-extension-samples/tree/main/source-control-sample)       | 原生资源组、多选命令、点击 Diff、Quick Diff、少量标题栏动作              | 原生 SCM 做高频快速路径，Svelte 做复杂深度任务      | 复杂冲突编辑、AI 回执和危险预览仍需 Svelte                   |
| [svn-scm](https://github.com/JohnstonCode/svn-scm)                                                                                                                                                        | 在 VS Code 原生 SCM 中提供状态、Quick Diff、Changelist、Commit           | 验证“简单 SVN 日常动作不必全部进入大 Webview”       | 其安全确认、范围隔离和多仓库约束不如本项目完整，不能直接复刻 |
| [GitLens](https://github.com/gitkraken/vscode-gitlens)                                                                                                                                                    | tabbed views、上下文 Inspect、按需详情、避免多个标题争夺空间             | Repository 分组、History/详情、当前对象上下文       | PR、worktree、branch graph 不是 SVN 日常模型                 |
| [GitButler](https://github.com/gitbutlerapp/gitbutler) / [Commit Editing](https://docs.gitbutler.com/features/branch-management/commits)                                                                  | 通过可见容器和拖放组织工作；复杂提交编辑有直接操作                       | Changelist 任务分组、把抽象规则转成可见文件集合     | virtual branches、无限 undo、历史重写不能映射到 SVN          |
| [lazygit](https://github.com/jesseduffield/lazygit)                                                                                                                                                       | 当前面板的一键动作、统一确认、过滤和帮助                                 | 上下文快捷提示、统一确认语法                        | 不能为了速度牺牲中文、可访问性和鼠标用户                     |
| [gitui](https://github.com/gitui-org/gitui)                                                                                                                                                               | 上下文帮助、键盘优先、响应式终端 UI                                      | `?` 当前区域帮助、真实快捷键发现性                  | 终端布局、Git staging/undo 语义不适用                        |

这些案例只证明某种交互模式在成熟产品中被采用，不证明它在本项目一定有效。采用前必须同时通过 SVN scope、安全确认、多仓库、external、本地降级和真实用户任务验证。

## 7. v0.0.17 与 v0.0.18 建议重排

### 7.1 v0.0.17：日常闭环可用性

建议将主题从“发现性与核心任务效率”进一步收敛为“日常闭环可用性”，交付顺序如下：

| 批次 | 交付内容                               | 完成标准                                                                     |
| ---- | -------------------------------------- | ---------------------------------------------------------------------------- |
| A    | 完成 Update 独立模块全链路             | 协议、Host、Webview、Mock、守卫、测试完整；旧 Repository Update 入口明确迁移 |
| B    | ScopeBar 提前收尾                      | 项目、范围、候选数、revision、来源可见且可展开复制                           |
| C    | Changes 唯一主操作 + Commit 紧凑模式   | 无竞争提交按钮；选择与数量一致；Commit 默认不重复展示完整选择控制台          |
| D    | Update 结果/Changes 冲突直达 Conflicts | 冲突数量一致；完成后一步进入正确 scope                                       |
| E    | Conflicts 步骤化 + AI 收入“需要帮助”   | 保存、验证、Resolve、下一个冲突顺序明确                                      |
| F    | Repository 分组与渐进展开              | 日常、分支集成、维护危险三层分开；默认不铺开高级项                           |
| G    | 形成性用户走查与修正                   | 5–7 名目标用户完成第 9 节任务；记录停顿点并至少迭代一次                      |

建议把“文件类型筛选 + 命名筛选预设”降为 v0.0.18：当前首先需要减少主任务决策，而不是为复杂列表增加更多持久状态。

“全局推荐下一步带”建议改成**上下文任务摘要**：冲突、锁定、过期等强状态可以全宽展示；普通推荐不要在每个模块永久占一行。

### 7.2 v0.0.18：理解、长列表与视觉打磨

建议范围：

1. 在简化后的真实流程上做三分钟引导。
2. 状态词就地解释。
3. History 加载更早与查询范围。
4. 文件类型筛选和命名预设，但先验证用户确有重复筛选需求。
5. Understanding 大范围进度。
6. AI AssistancePanel 跨模块一致性。
7. 标题、间距、边框、空态、Tooltip 与快捷帮助统一。
8. 720×480、200%、Light/Dark/High Contrast 和真实读屏走查。

### 7.3 暂不建议投入

- 新的大首页或仪表盘；
- 更多一级模块；
- 为每个任务增加独立 AI 按钮；
- 用教程解释仍然复杂的页面；
- 没有用户数据支撑的高度可配置布局；
- 对 SVN 不具备可靠恢复语义的“一键撤销”承诺；
- 仅为保持历史测试数量而新增或删除测试。

## 8. 实现落点建议

| 体验改动           | 主要代码位置                                                                                                               | 约束                                                  |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| ScopeBar 完整范围  | `src/webview/components/svn/ScopeBar.svelte`、协议 ScopeView、Host presentation                                            | 长路径键盘可达；不暴露敏感绝对路径；scope 只能缩小    |
| Changes 主操作收敛 | `src/webview/features/changes/ChangesModule.svelte`                                                                        | 显示数量与 payload 一致；blocked/external 不静默加入  |
| Commit 紧凑模式    | `src/webview/features/commit/CommitModule.svelte`                                                                          | 草稿、建议、回执、确认 token 契约不变                 |
| Update 独立闭环    | `src/protocol/workbenchProtocol.ts`、`WorkbenchController` 对应领域拆分、`src/webview/features/update/UpdateModule.svelte` | 协议改动同步 Host/Webview/Mock/类型守卫/测试          |
| Conflicts 步骤条   | `src/webview/features/conflicts/ConflictsModule.svelte`、冲突领域状态                                                      | 保存不等于 Resolve；AI 只生成建议；草稿三选一守卫保留 |
| Repository 分组    | `src/webview/features/repository/RepositoryModule.svelte`                                                                  | 不恢复巨型首页；高级危险项默认折叠                    |
| History 加载边界   | `src/history/svnHistory.ts`、Host snapshot、`HistoryModule.svelte`                                                         | “没有匹配”与“尚未加载”分开；可取消                    |
| 上下文帮助         | 列表共享组件与 i18n                                                                                                        | 从真实绑定生成或单一来源维护；IME 不被劫持            |
| AI 渐进展开        | 建议抽取 `AssistancePanel` 共享组件                                                                                        | 来源如实；模型未配置不影响核心流程                    |

工程上应避免一次性重写页面。每个批次先提取一个可复用交互，再迁移对应模块并补测试；不对无关文件批量格式化，不扩大 `WorkbenchController.ts` 中可独立测试的纯逻辑。

## 9. 验收指标：把“好用”变成可检查结果

### 9.1 形成性用户任务

参与者：5–7 名用过 SVN、未使用过 SVN Workbench 的开发者。每人完成：

1. 首次打开非 SVN 文件夹，恢复到可查看一个工作副本修改。
2. 从 Explorer 选中一个目录，找到 3 个可提交文件并查看其中 1 个 Diff。
3. 排除生成物，写提交说明，走到最终确认前，不执行真实提交。
4. 检查远端更新并执行到隔离测试工作副本；更新产生冲突后进入正确冲突文件。
5. 编辑冲突、保存、标记解决并进入下一个冲突。
6. 查找指定作者/日期的较早 revision，并确认是否已加载完整范围。
7. 在 AI 未配置状态下重复核心提交准备流程。
8. 在 5000 文件数据集查找、筛选并选中指定文件。

### 9.2 建议指标

| 指标            | v0.0.17 目标                                  | 说明                           |
| --------------- | --------------------------------------------- | ------------------------------ |
| 首次任务成功率  | ≥80% 无口头提示完成前 5 个任务                | 形成性目标，不宣称统计显著     |
| 日常路径成功率  | 5/5 用户完成“看 Diff → 到提交确认前”          | 任何范围扩大都为阻断失败       |
| 主动作定位时间  | 中位数 ≤10 秒                                 | 从页面稳定显示开始计时         |
| 提交准备决策点  | 从 Changes 到意向单不超过 3 次主要决定        | 搜索、输入文字不计为决策点     |
| 写操作确认次数  | 每个操作 1 次意向单确认                       | 预览生成不算确认               |
| Update 冲突转入 | 1 次动作进入正确 Conflicts scope              | 数量与路径一致                 |
| 任务后信心评分  | 7 分制中位数 ≥5                               | 问“我确信这次只会影响预期文件” |
| 关键停顿        | 同一文案/控件不得使 2 名以上用户停顿 >10 秒   | 出现即进入修正文案或结构清单   |
| 安全契约        | 现有 scope/token/revision/AI/凭据门禁全部通过 | 易用性不能以降低安全换取       |

### 9.3 自动化与人工验收分工

- 单元/组件/Extension Host：证明范围、状态、token、失败与恢复分支正确。
- Webview E2E：证明主路径、焦点、IME、滚动和动作数量一致。
- 视觉回归：证明三主题、三尺寸与 200% 缩放没有信息层级退化。
- 真人走查：证明用户能找到入口、理解文案、完成任务。
- 真实 SVN：证明 Update/Conflict/Commit 的结果和恢复路径符合实际工作副本。

不得用自动化测试、axe 或截图替代真人走查，也不得用用户“最终完成了”掩盖长时间停顿和反复返回。

## 10. 风险与控制

| 风险                            | 可能后果                          | 控制方式                                            |
| ------------------------------- | --------------------------------- | --------------------------------------------------- |
| 简化时隐藏了专家能力            | 老用户认为功能退化                | 只降级到“更多/高级”，不删除；提供命令面板直达       |
| 自动预选造成范围误解            | 用户担心静默扩大                  | 默认仅建议，不自动新增；数量与文件列表持续一致      |
| Native SCM 与 Svelte 状态不一致 | 两处显示不同选择或草稿            | 继续以 Host session 为权威，Webview 不伪造 identity |
| 推荐下一步判断错误              | 用户被带到错误任务                | 只对冲突/锁定/过期等确定状态强推荐；允许忽略        |
| AI 折叠后使用率下降             | 可选增强不易发现                  | 在相关输入旁保留单一入口；不做全局广告              |
| 页面拆组件引入回归              | 状态或焦点丢失                    | 伴随体验批次小步提取；组件/E2E 覆盖焦点与草稿       |
| 参考 Git 产品过度               | 引入 SVN 不存在的 stage/undo 心智 | 每条案例明确只借交互模式，领域语义由 SVN 契约决定   |

## 11. 最终建议排序

如果只能做五件事，按以下顺序：

1. Changes 只保留一个准确主操作，消除“所选”和“当前范围”竞争。
2. ScopeBar 持续显示项目、范围、候选数、revision 与来源。
3. 完成独立 Update 的结果闭环，冲突一步直达。
4. 冲突页步骤化，并将多个 AI/建议入口收进“需要帮助”。
5. 用 5–7 名真实目标用户走查决定 v0.0.18，而不是继续凭功能清单打磨。

完成这五项后，SVN Workbench 的核心竞争力会更清晰：它不是“功能最多的 SVN 页面集合”，而是“在 VS Code 里把复杂 SVN 操作做得更确定、更省心的工作台”。
