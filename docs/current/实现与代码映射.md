# SVN Workbench 实现与代码映射

> 文档身份：`current-implementation-map`
>
> 状态：CURRENT。
>
> 负责：领域契约、Host/Webview 边界、协议、主要源码位置和测试位置。
>
> 不负责：产品优先级见 [`产品与功能基线.md`](./产品与功能基线.md)；交互规范见 [`设计与交互基线.md`](./设计与交互基线.md)；版本状态见 [`../releases/`](../releases/)。

## 1. 权威边界

先按 [`../README.md`](../README.md) 判断问题类型，再使用负责该领域的主权威：

- 当前协议、源码路径和实际行为：实际源码、协议类型、自动化测试与本文。
- 产品范围和优先级：[`产品与功能基线.md`](./产品与功能基线.md)。
- 中文、布局和交互：[`设计与交互基线.md`](./设计与交互基线.md)。
- 测试和交付门禁：[`测试与验收基线.md`](./测试与验收基线.md)。
- 已发布事实：对应版本 manifest 与 accepted evidence。

产品基线是规范目标，不自动证明功能已经实现。归档中的完整页面结构、旧里程碑和旧技术选型不具有当前权威性。

## 2. 不可破坏的领域契约

### 2.1 操作范围

所有命令先解析统一上下文：

```ts
interface ResolvedCommandContext {
  repositoryRoot: string;
  repositoryUuid: string;
  operationScope: OperationScope;
  selectedFiles: string[];
  statusSnapshot: SvnStatusSnapshot;
  capabilities: CommandCapabilities;
}
```

必须遵守：

- 文件入口只允许影响该文件。
- 文件夹入口只允许影响该目录及其子目录。
- 多选范围是所选路径的去重并集，父目录已选时合并其子路径。
- 筛选、搜索、模板、Changelist 和 AI 只能缩小范围。
- 嵌套工作副本与 `svn:externals` 默认形成独立仓库边界。
- 混合仓库选择不能提交为一个 revision，必须按仓库拆分。
- 写操作执行前再次校验绝对路径、仓库 UUID 和范围哈希。

### 2.2 状态模型

内部状态至少区分：

- `modified`
- `added`
- `deleted`
- `missing`
- `replaced`
- `conflicted`
- `unversioned`
- `ignored`
- `external`
- `obstructed`
- `lockedBySelf`
- `lockedByOther`
- `outOfDate`

文本状态、属性状态、远端状态、锁状态和仓库归属应分别保存，不能压成一个显示字符。UI 可以按优先级显示装饰，但领域层不能丢失原始维度。

提交选择决策以 `(status, propStatus)` 二元组为输入：仅 SVN 属性变化（`status=normal`、`propStatus=modified`）独立于普通 `normal` 判断，自 v0.0.3 起按 `propertyModified` 状态策略默认推荐提交，不再作为普通 `normal` 排除（`src/commit/commitSelectionRuleEvaluator.ts`；金样期望已随该有意行为修正更新）。`conflicted`、`obstructed`、`incomplete` 出现在任一维度（文本状态或属性状态）即进入不可覆盖的阻止结果；提交计划构建器（`src/commit/commitPlanBuilder.ts`）按同一二元组判断可提交性，仅属性变化只进入 `commitPaths`。

### 2.3 多仓库与 Changelist

- 每个工作副本创建独立 repository model。
- Svelte Changes 按冲突、Changelist、修改、新增、删除、缺失和未版本化分组。
- Changes 和智能提交模块共享同一份 Host 草稿来源。
- AI 拆分结果只能生成 Changelist 建议，用户可以移动或移出文件。
- Changelist 草稿跨会话恢复时必须重新采集状态并检查过期路径。

## 3. SVN 命令契约

### 3.1 通用规则

- 所有命令通过一个 `SvnCommandRunner` 执行，禁止 UI 层直接启动进程。
- 优先解析 `--xml`，不要依赖本地化终端文案。
- 参数使用数组传递，不拼接 shell 命令字符串。
- 命令记录需要脱敏密码、token、认证缓存路径和用户隐私数据。
- 长任务必须支持阶段、耗时、输出入口和取消信号。
- 取消、失败或工作副本状态变化后，必须重新执行状态采集才能重试。

### 3.2 主要能力与命令

| 能力              | 推荐命令/机制                                                    | 关键约束                                                                                                                       |
| ----------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| 本地状态          | `svn status --xml`                                               | 限定工作副本和操作范围                                                                                                         |
| 远端检查          | `svn status --show-updates --xml`                                | 失败不能伪装成“无更新”                                                                                                         |
| 信息与归属        | `svn info --xml`                                                 | 读取 root、URL、UUID、revision                                                                                                 |
| 更新              | `svn update <paths>`                                             | 执行前展示范围和冲突风险                                                                                                       |
| 提交              | `svn commit <paths> -F <file>`                                   | 消息使用临时文件；执行最终远端检查                                                                                             |
| Diff              | `svn diff`、`svn cat -r BASE`、Svelte 只读 Diff 与 `vscode.diff` | Webview 只读审阅；原生对比左侧为 Host 管理的只读 BASE 虚拟文档，右侧为范围内工作副本；二进制、截断、无 BASE 和过期目标安全拒绝 |
| 历史              | `svn log --xml -v`                                               | Changed Paths 保留 revision 来源                                                                                               |
| Blame             | `svn blame --xml`                                                | 单文件入口                                                                                                                     |
| Add/Delete/Revert | 对应 SVN 子命令                                                  | Delete/Revert 显示精确清单和恢复方式                                                                                           |
| Lock/Unlock       | 对应 SVN 子命令                                                  | 展示锁所有者与注释                                                                                                             |
| 属性              | `proplist/propget/propset/propdel`                               | 修改前显示原值和目标值                                                                                                         |
| Cleanup           | `svn cleanup`                                                    | 默认不删除未版本化或忽略文件                                                                                                   |
| Switch/Relocate   | 对应 SVN 子命令                                                  | 只在合法仓库上下文显示；操作前预检                                                                                             |
| Branch/Tag        | `svn copy`                                                       | 展示来源、目标 URL 和提交说明                                                                                                  |
| Merge             | `svn merge`                                                      | 先预检，冲突转入 Svelte 冲突与合并模块                                                                                         |

## 4. UI 表面契约

| 任务                                             | 首选表面                                       |
| ------------------------------------------------ | ---------------------------------------------- |
| 持续查看状态、分组、Changelist、提交草稿         | Svelte Changes 模块                            |
| 更新当前范围（预览/执行/冲突直达，v0.0.17 独立） | Svelte Update 模块                             |
| 文件与修订差异                                   | Svelte Diff 模块                               |
| 文本冲突编辑                                     | Svelte 冲突与合并模块                          |
| 简单选择与轻量确认                               | Svelte Dialog / Command / Context Menu         |
| 密码输入                                         | Svelte 安全输入；Secret 只传给 Host，不回显    |
| 长任务                                           | Svelte Progress/Task 状态；Host 管理取消和日志 |
| 配置项                                           | Svelte Settings 模块                           |
| 智能提交、变更解读、按改动意图拆分、冲突意图解释 | 对应 Svelte feature module                     |
| 仓库浏览                                         | Svelte Repository 模块                         |

所有可见业务 UI 统一使用 Svelte 5。自 v0.0.5 起，每个功能模块拥有独立 Webview 窗口，一个 Workbench Shell 根据命令直接进入目标模块；不得恢复一个同时挂载所有能力和全部状态的巨型页面，窗口内不再提供模块总览列表（Rail 已移除）。模块内任务导航、空态/错误/设置页的关联入口保留。

自 v0.0.5 起，模块窗口由统一窗口管理器（`src/extension/workbench/workbenchWindowManager.ts`）按 `WorkbenchModuleId` 惰性创建、单例复用的 `WorkbenchController` 承载；同模块重复打开复用窗口并加载新目标，不同模块互不顶替，关闭后按需重建，扩展停用时统一释放。跨模块打开请求（命令/右键入口、Webview 的 `open-module`、`open-diff`、`history/compare`）由控制器回传窗口管理器，经 `src/extension/workbench/workbenchRouting.ts` 纯路由逻辑路由到目标模块窗口，源窗口保持原快照与操作范围。`svnWorkbench.diff.openMode` 支持 `sameGroup | beside`：默认 `sameGroup` 在当前编辑组激活目标标签，`beside` 在旁侧编辑组激活；缺失或非法配置安全回退到 `sameGroup`。同一 Diff 目标重复打开只 reveal，不重新初始化；目标文件、范围或修订变化时建立新会话。控制器收到非本模块会话请求且未接线时明确拒绝（`src/extension/workbench/WorkbenchController.ts`、`src/extension/workbench/workbenchRouting.ts`）。

Working Copy ↔ BASE 页面可发送 `diff/open-in-editor`。Host 重新校验当前范围，读取工作副本和 `svn cat -r BASE`，通过 `NativeDiffContentProvider` 以不透明随机 URI 提供只读 BASE，并调用 `vscode.diff`；右侧使用实际工作副本 URI。BASE 内容与 URI 在会话替换、面板关闭或控制器释放时失效。修订比较、二进制、超过 5 MB、无文本差异、无 BASE 或过期目标不打开原生 Diff，并返回中文原因与恢复动作。

### 4.1 Workbench 协议与会话隔离

`WORKBENCH_PROTOCOL_VERSION` 当前为 `2`。Host 为每次安全上下文或 Diff 目标变化生成新的 `sessionId`；所有 `workbench/action` 必须携带并通过 `sessionId + repositoryUuid + scopeHash + taskId` 校验，任一不一致均作为过期上下文拒绝。Webview 在新的 `app/initialize` 接管后忽略旧 `sessionId` 的延迟快照、进度和结果。`diff/open-in-editor` 同时登记在 `WebviewAction` 字面量联合与 `webviewActions` 运行时清单中；协议 action 变化必须同步检查 Host、Webview、Mock、消息守卫和测试。

多窗口共享 SVN 安全上下文（`src/security/svnSecurityContextRegistry.ts`）：按 repository identity 引用计数管理认证与证书信任；窗口会话创建时 `acquire`、替换或关闭时 `release`，仅最后一个引用消失才清除共享上下文；`sync` 采用合并语义避免空会话覆盖其他窗口凭据；显式清除认证时向相关窗口广播失效事件（`WorkbenchController.handleSecurityInvalidated` 重新读取 SecretStorage 并刷新设置页快照，不重载其他模块以保留未提交输入）。`moduleId + taskId + operationScope` 在每个窗口独立保存，跨模块路由只保持（不扩大）源窗口范围。

## 5. 安全与恢复契约

### 5.1 认证与证书

- 密码不得写入 settings、工作区配置或日志。
- 可使用 VS Code SecretStorage 保存扩展管理的 secret，但要兼容 SVN 自身认证缓存。
- 证书确认必须显示主机、SHA-256 指纹、错误原因和有效期。
- “仅本次信任”与“永久信任”必须明确区分。
- 认证失败、证书失败、代理失败、DNS 失败和离线状态不能合并成一个错误。

### 5.2 破坏性操作

Revert、Delete、Switch、Relocate、Merge 和从历史恢复文件必须展示：

- 精确路径清单。
- 本地未保存或未提交内容。
- 是否可以撤销以及如何恢复。
- 将执行的 SVN 命令或动作。
- 明确的二次确认。

必要时先提供恢复 Patch。不要把“本地搁置”描述为 SVN 标准 shelf；它是插件管理的可恢复 Patch。

### 5.3 工作副本恢复

- 识别锁定、未完成 update/merge、tree conflict 和 obstructed 状态。
- Cleanup 页面展示安全默认参数，不默认删除 ignored/unversioned 文件。
- 恢复后刷新 SCM、候选文件、revision、冲突和 AI 结果有效性。

## 6. AI 契约

### 6.1 处理顺序

```text
OperationScope
  -> SVN 状态与仓库边界
  -> 本地确定性规则
  -> 敏感信息 / 生成物 / 大文件 / 调试代码扫描
  -> 裁剪后的 AI 上下文
  -> 结构化结果校验
  -> 用户确认
```

### 6.2 结果要求

- 文件级判断包含 `path`、分类、理由和置信度。
- 审查结论包含文件、行号、证据、严重度和置信度。
- 拆分建议包含文件集合、提交意图、建议说明和依赖关系。
- 冲突建议包含两侧意图、候选结果、风险和验证步骤。
- 结果来源统一文案（v0.0.9 §3.1）：`local-rule` 为“本地检查”、`configured-model` 为“模型建议”、`local-rule-fallback` 为“模型不可用，已保留本地结果”，每条结果标记来源，页面级总来源不得掩盖混合结果。
- 纯本地能力不得表述为“AI 已审查”“智能分析”：本地检查/影响分析统一收敛进变更解读（`understanding`）；按目录/文件类型分组（`changelists`）作为无差异时的诚实降级；设置页场景列表只展示有真实调用链的场景（`AI_VISIBLE_USAGE_SCENARIOS` 排除 `conflictMerge`）。
- 提交说明生成（v0.0.9 §4）：`commit/generate-message` 的结果只进入
  `CommitSnapshot.messageSuggestion` 建议区，绝不写回 `CommitSnapshot.message`；
  生成、失败、超时、取消、降级、过期均不覆盖用户草稿。采用必须显式经
  `commit/adopt-suggestion`（`insert-blank-fields` 只补充空白字段，`replace` 替换前校验并备份可撤销），
  `commit/undo-suggestion-replace` 恢复替换前草稿，`commit/discard-suggestion` 放弃建议；
  建议绑定 `repositoryUuid + scopeHash + candidateHash + generatedAt + model`，
  范围/候选变化后建议只读（stale），采用前 Host 重新校验候选（`isCommitMessageSuggestionStale`），
  替换备份在用户手动编辑/套用模板/再次采用/放弃后失效。
  纯逻辑在 `src/commit/commitMessageSuggestion.ts`；协议 `CommitMessageSuggestion` 与 `CommitSnapshot.messageSuggestion`；
  Host 接线 `WorkbenchController`（generate/adopt/undo/discard 动作、`CommitSessionState.messageSuggestion` 与
  `messageSuggestionReplaceBackup`、快照 stale 标记与持续撤销反馈）；
  Webview `CommitModule.svelte` 建议区（来源/元数据声明、行级差异、插入空白字段/替换/复制/放弃、替换前字符数确认与撤销入口）；
  Mock `src/webview/mocks/mockWorkbench.ts`；项目切换守卫 `projectSwitchGuard.ts`（`hasCommitMessageSuggestion`）。
- 模型返回路径必须经过仓库与范围校验；虚构路径和范围外路径直接丢弃。
- 提交文件选择请求只发送当前范围候选的相对路径、SVN 状态、文件类型和本地规则结论（`localDecision`、`safetyLocked`），不发送本地绝对路径（`src/ai/commitSelectionAi.ts`）。
- AI 选择建议必须经本地边界校验：本地阻止项、强制排除项和用户明确配置的排除结果不得被 AI 改为推荐（`src/ai/aiResultValidator.ts` 的 `enforceAiSelectionLocalBoundary`）。
- AI 不得直接执行 commit、update、resolve、revert、delete、switch 或 merge。

### 6.3 上下文与过期

每个 AI 结果至少绑定：

- repository UUID
- operation scope hash
- 文件内容或 diff hash
- working copy revision
- 生成时间与模型

任一关键值变化后结果标记为过期，只能查看或重新生成，不能直接采用。

### 6.4 隐私与降级

- 外发前显示文件数、字符预算、模型和数据类型。
- 默认不发送范围外文件、完整仓库历史、认证信息和本地绝对路径。
- 团队记忆优先保存本地摘要，并提供查看来源、清除缓存和关闭入口。
- AI 未配置或调用失败时，保留范围保护、敏感规则、生成物检测和全部手动 SVN 流程。
- 提交文件选择场景中，AI 调用失败时保留用户当前选择，展示失败原因与“应用本地规则”恢复动作，不静默替换为另一来源的结果。

## 7. 旧 AI 模块入口收敛（v0.0.12 批次 C）

`ai-review`、`impact`、`agent` 一级模块/命令/菜单已按 ds1 决策移除（不保留别名）；
`understanding`（变更解读）为唯一变更解读主路径。本地检查与影响分析引擎
（`src/ai/changeIntelligence.ts`，`AiReviewSnapshot`/`ImpactSnapshot` 领域契约）
保留供变更解读本地适配。旧深链（moduleId ai-review/impact/agent）安全失效：
消息守卫与运行时 moduleIds 清单已移除对应项，旧会话/深链被拒绝。设置页场景列表
与 README/CHANGELOG/docs/current 已同步。

## 8. 当前功能、代码与证据映射

<!-- prettier-ignore-start -->
| 功能                                | 主要源码                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 主要测试                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 引入版本                                        | 当前状态                               | 版本证据                             |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | -------------------------------------- | ------------------------------------ |
| 操作范围与多工作副本边界            | `src/scope/`、`src/scm/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `tests/unit/`、Extension Host                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | v0.0.1                                          | 已实现                                 | `releases/v0.0.1`                    |
| 项目边界与活动项目解析（v0.0.7）    | `src/scope/projectIdentity.ts`、`src/scope/projectResolver.ts`、`src/scope/workingCopyClassification.ts`、`src/extension.ts`（`resolveCommandTarget` 与多根项目选择器）、`OperationScope.project`、`WorkbenchScopeView.projectName/projectRootIsFallback/projectWorkingCopyRelativePath`、`src/webview/components/svn/ScopeBar.svelte`、`WorkbenchController.classifyFolderWorkingCopyBinding`、`src/diagnostics/environmentDiagnostics.ts` | `tests/unit/projectIdentity.test.ts`、`tests/unit/projectResolver.test.ts`、`tests/unit/workingCopyClassification.test.ts`、`tests/components/ScopeBar.test.ts`、`tests/unit/workbenchArchitecture.test.ts` | v0.0.7 | 已发布（详见 §8.2、§8.3） | `releases/v0.0.7` |
| SVN 命令、解析和错误分类            | `src/svn/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `tests/unit/svnCommandRunner.test.ts`、真实 SVN                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | v0.0.1；v0.0.2 增强                             | 开发中                                 | `releases/v0.0.2/变更映射.md`        |
| 更新与安全提交                      | `src/update/`、`src/commit/`、`.svn-workbench.json` 统一读写层 `src/config/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 单元测试、Extension Host                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | v0.0.1；v0.0.2 增强                             | 开发中                                 | 同上                                 |
| 提交选择规则（本地决策与解释模型）  | `src/commit/commitSelectionRules.ts`、`commitSelectionRuleResolver.ts`、`commitSelectionRuleEvaluator.ts`；运行时解析与缓存失效 `src/commit/commitSelectionRuleService.ts`（含仓库级保存/恢复默认编排）；统一候选入口 `src/commit/commitCandidateCollector.ts`（可选 rules 参数，缺省内置默认）；Host 失效链路 `src/extension/workbench/commitSelectionInvalidation.ts`；兼容包装 `src/commit/generatedFilePolicy.ts`；VS Code 设置适配 `src/config/commitSelectionSettings.ts`；设置页快照段/保存校验/对象级合并 `src/commit/commitSelectionSettingsSupport.ts`；配置键删除辅助 `src/config/svnWorkbenchConfig.ts`；协议 `settings/selection` 与 `SettingsSnapshot.selection`、`commit/apply-local-rules` 与 `CommitSnapshot.selectionAi/feedback/ai.failed/ai.stale`、`WorkbenchFileView.evaluation` `src/protocol/workbenchProtocol.ts`；AI 边界 `src/ai/aiResultValidator.ts`（`enforceAiSelectionLocalBoundary`）与 `src/ai/commitSelectionAi.ts`（请求携带本地规则结论）；设置页 UI（第四标签、作用域、状态策略、路径规则与 Webview 端本地预览）`src/webview/features/settings/SettingsModule.svelte`、`src/webview/features/settings/SelectionTask.svelte`；提交页来源拆分（“应用本地规则”始终可用、“获取 AI 建议”绑定 AI 配置、未配置显示“配置 AI”入口、候选决策/来源/安全锁定展示、AI 失败保留选择与恢复动作、过期标记、规则更新一次性反馈）；V014-D Commit 紧凑模式（首屏仅“待提交 N 个文件”摘要条/提交说明/“本地检查”摘要/唯一主操作“预览提交 N 个文件”，完整文件选择与策略、AI 建议与外发回执、团队规则详情、完整命令与证据按需展开，草稿/建议/回执/意向单与预览 token 契约不变）`src/webview/features/commit/CommitModule.svelte`；中文术语 `src/webview/i18n/terminology.ts`；Mock `src/webview/mocks/mockWorkbench.ts`                                                                                                                                                                                                                                 | `tests/unit/commitSelectionRules.test.ts`、`commitSelectionRuleResolver.test.ts`、`commitSelectionRuleEvaluator.test.ts`、`commitSelectionGolden.test.ts`、`commitSelectionRuleService.test.ts`、`commitSelectionRuntime.test.ts`、`aiSelectionLocalBoundary.test.ts`、`aiSelectionResultValidation.test.ts`、`commitPlanBuilder.test.ts`、`commitSelectionInvalidation.test.ts`、`commitSelectionSettingsSupport.test.ts`、`workbenchProtocol.test.ts`、`svnWorkbenchConfig.test.ts`、`tests/components/SettingsModule.test.ts`、`tests/components/CommitModule.test.ts`、`tests/webview-e2e/workbench.spec.ts`、`tests/webview-e2e/chinese-scroll.spec.ts`、`tests/webview-e2e/visual-accessibility.spec.ts`、`tests/webview-e2e/list-operations.spec.ts`、`tests/webview-e2e/page-screenshots.spec.ts`（V014-D 紧凑结构同步） | v0.0.3                                          | 已发布（含代码审查 P1 修复与正式证据） | `releases/v0.0.3`                    |
| Changelist 与提交拆分               | `src/changelist/`、`src/commit/commitSplit*`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 单元测试、Webview                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | v0.0.1                                          | 已实现                                 | `releases/v0.0.1`                    |
| Diff、历史、冲突与恢复              | `src/history/`、`src/conflict/`、`src/repository/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 单元测试、Webview、真实 SVN                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | v0.0.1                                          | 已实现                                 | `releases/v0.0.1`                    |
| Diff 渲染（`@pierre/diffs` 适配层） | `src/webview/features/diff/DiffView.svelte`（vanilla 挂载、`cspCompatObserver.ts` CSP 兼容观察器、`diffLanguage.ts` 语言子集与 `vendor/` shim、unified/split 切换、失败降级 MergeView）；三主题映射 `src/webview/styles/diff-theme.css`；迁移后的 `DiffModule.svelte`（Working/BASE 两侧全文 + 修订比较 patch 直渲）；Spike 工程 `tests/spike/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `tests/components/DiffModule.test.ts`、`tests/webview-e2e/page-screenshots.spec.ts`（02-diff axe 基线）、`tests/spike/e2e/spike.spec.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | v0.0.4                                          | 已发布（发布证据已归档）               | `releases/v0.0.4`                    |
| 差异底座重整与可测基线（v0.1.0） | `src/webview/features/diff/diffViewAdapter.ts`（`mountDiffView` 单一生命周期：observer 注册 → FileDiff 创建 → Editor attach → 统一幂等 dispose；释放顺序 observer disconnect → Editor detach/cleanUp → FileDiff.cleanUp → 清空容器；修复 Editor detach/cleanUp 缺口）、`diffErrorTaxonomy.ts`（10 种 `DiffErrorKind` 结构化错误分类，每种含发生了什么/可能原因/现在能做什么，`DiffStageError` 携带 stage）、`DiffModule.svelte`（统一工具区：变更块 X/Y、上一处/下一处（Alt+↑/↓）首尾非阻塞反馈、显示设置 popover 收敛 split/unified 与上下文展开、徽章正在编辑工作副本、保存按钮保存到工作副本与保存中/已保存文字态、结构化降级 + 重试渲染/重试高亮、编辑态 unified 临时强制 split 退出恢复）、`DiffView.svelte`（薄化为 diffStyle/expandUnchanged/retryToken/onHighlightError props）、`diffHunks.ts` `computePatchHunks`（修订比较 @@ 头解析导航）、`src/webview/mocks/diffFixtures.ts`（确定性 fixture，ID 字节级一致）、`src/webview/bridge/vscodeBridge.ts` `isWorkbenchEnvelope`（拦截 pierre Editor tokenizer 经 globalThis.postMessage 的自调度消息，修复误判协议版本不兼容终止会话）、mock `?module=diff&diffFixture=<id>` 与 currentMockSessionId 随目标切换修正、`scripts/measure-diff-performance.js`、`src/webview/i18n/terminology.ts` diffViewLabels、`src/webview/styles/diff-theme.css`；`@pierre/diffs` 锁定精确 1.3.4 | `tests/unit/pierreCapability.test.ts`（能力矩阵静态契约）、`tests/unit/diffErrorTaxonomy.test.ts`、`tests/unit/diffViewAdapter.test.ts`、`tests/unit/diffFixtures.test.ts`、`tests/unit/vscodeBridge.test.ts`、`tests/components/DiffModule.test.ts`（v0.1.0 工具区/导航/降级/强制分栏）、`tests/unit/diffHunks.test.ts`（computePatchHunks）、`tests/webview-e2e/workbench.spec.ts`（v0.1.0 差异主路径）、`tests/webview-e2e/diff-edit-csp.spec.ts`、`npm run test:diff-performance` | v0.1.0（开发中） | 开发中 | 规划 `releases/v0.1.0` |
| 统一冲突差异视图与角色映射（v0.1.1 批次 A） | 领域 `src/conflict/conflictDiffModel.ts`（纯函数品牌类型 `ConflictFileIdentity`/`ConflictRegionIdentity`/`ContentHash`，`hashText` FNV-1a，`buildConflictFileIdentity`/`buildConflictRegionIdentity`，`parseConflictRegions` 行级严格解析、结构化 `ConflictParseError`（missingSeparator/unfinished/nested 等，不猜测不修复），`buildConflictFileModel` 生成稳定 identity/hash 与四角色显示模型 `我的修改（本地）`/`对方修改（仓库 rN）`/`共同基线（BASE）`/`合并结果`，`buildPierreUnresolvedInput` 构造 Pierre 输入；CRLF/BOM/末尾换行透传；SVN 真实顺序 `<<<<<<< .mine` → `||||||| .rBASE?` → `=======` → `>>>>>>> .rN` 按位置映射 Mine/Theirs，`fixtures.ts` 确定性 fixture）、`src/conflict/fixtures.ts`（SVN/Git、多块、CRLF、无 BASE、损坏、超长行、5000 行及交换映射证明）、复用 `src/conflict/conflictMerge.ts` | `tests/unit/conflictDiffModel.test.ts`（角色映射/排序/CRLF/BOM/EOL/无 BASE/损坏/超长/多块/5000 行/四角色/hash 失效/RegionIdentity 稳定/交换证明/Pierre 输入）、`tests/unit/conflictMerge.test.ts`（扩展 Git/CRLF/损坏） | v0.1.1（开发中） | 开发中 | 规划 `releases/v0.1.1` |
| 统一冲突差异视图薄适配（v0.1.1 批次 B） | `src/webview/features/conflicts/ConflictDiffView.svelte`（Svelte 5，`$props` 接收 `workingText/relativePath/language/fileIdentity`，`$effect` 挂载 `mountConflictDiffView`，`buildPierreUnresolvedInput` 先做 fail-closed 校验损坏 marker 不挂载半态，暴露 `focusConflict/getBlockProgress/getControlledText/cleanup`，IME `isComposing` 保护，`role=group`/`aria-label` 状态不只依赖颜色；角色条 `我的修改（本地）`/`对方修改（仓库）`/`共同基线（BASE）`/`合并结果` 及块进度 `X/Y`）、`src/webview/features/conflicts/conflictDiffViewAdapter.ts`（`mountConflictDiffView`/`createChineseActionRenderer`/`shouldRebuildConflictView`，先校验 Pierre 输入，`new UnresolvedFile({theme,themeType:"system",mergeConflictActionsType:中文三动作, onMergeConflictAction})` + `render({file:{name,contents},containerWrapper})`，复用 `installDiffCspCompatibilityShim` + `observeDiffContainer/ShadowRoot` + `installDiffErrorObserver`，`dispose` 清理 observer/instance/DOM，payload 含 `resolution+conflict{conflictIndex,startLineIndex,separatorLineIndex,endLineIndex,baseMarkerLineIndex}`，绝不发起 Host 写操作，`VirtualizedUnresolvedFile` 不按名类推） | `tests/unit/conflictDiffViewAdapter.test.ts`（SVN/Git/多块/CRLF/无 BASE/超长行输入构造、CRLF 透传、损坏结构化错误、三动作标签与 payload 结构）、`tests/components/ConflictDiffView.test.ts`（mock UnresolvedFile：SVN 中文三动作可见可点击、回调 payload 完整性、Git/多块/CRLF/无 BASE/超长行挂载、损坏 fail-closed 不挂载半态并展示错误、销毁重建无泄漏）、`tests/unit/conflictDiffModel.test.ts`（复用 V011-A 纯领域校验） | v0.1.1（开发中） | 开发中 | 规划 `releases/v0.1.1` |
| 统一冲突差异视图页面重排（v0.1.1 批次 D） | `src/webview/features/conflicts/ConflictsModule.svelte`（顶部路径/revision/剩余数、紧凑导航上/下一个文件与上/下一个块+块X/Y、固定角色说明四角色不只颜色、主体 ConflictDiffView 接线+受控草稿同步 Host `conflict/draft-update`、查看来源折叠区与需要帮助折叠区默认收起、底部草稿/保存/Resolve 保留、打开自动聚焦首块、列表筛选不改scope） | `tests/components/ConflictsModule.test.ts`、`tests/components/ConflictsModuleV011D.test.ts`（导航/角色/折叠/顶部可见） | v0.1.1（开发中） | 开发中 | 规划 `releases/v0.1.1` |
| 统一冲突差异视图安全降级（v0.1.1 批次 E） | `src/webview/features/conflicts/ConflictsModule.svelte`（fail-closed 分支：`isFallbackContent/fallbackContentReason` 由 `readError||truncated||content===undefined` 判定二进制/截断/非法编码，`data-testid=content-fallback-warning` 显示真实 `readError` 并提供 使用简化编辑器/在编辑器中打开/导出草稿；`diffErrorInfo/useSimplified` 承接 ConflictDiffView `onError` 渲染异常，`data-testid=conflict-fallback-warning` 显示真实 `message/category`；`useSimplified` 切换显示 `simplified-fallback-notice`，草稿经 `snapshot.selected.draft.hasDraft` 始终可见可导出；底部草稿/复制/导出/保存/Resolve 均不自动触发；过期复用 `isStaleConflictAction`；mock `mockConflictAdvice` 保持 `local-rule` 不标为 AI）、`src/webview/mocks/mockWorkbench.ts`（`?conflictScenario=damaged|binary|truncated|missing` 分支，`workingContent` 与 `workingExtra:{truncated,readError}` 透传，演示正常/损坏/二进制/截断/缺失） | `tests/components/ConflictsModuleFallback.test.ts`（截断 fallback 保留草稿可切简化编辑器且不自动 save/resolve、二进制显示真实原因提供在编辑器中打开、草稿失败不丢弃仍可导出）、`tests/components/ConflictsModuleDraft.test.ts`（夹具改为合法 marker 防误判损坏）、`tests/components/ConflictsModule.test.ts`/`ConflictsModuleV011D.test.ts`/`ConflictDiffView.test.ts` 回归 | v0.1.1（开发中） | 开发中 | 规划 `releases/v0.1.1` |
| 统一冲突差异视图 E2E 与文档（v0.1.1 批次 F） | `tests/webview-e2e/conflict-v011-f.spec.ts`（§3.6 六项：三种结果草稿变化且仅 `conflict/draft-update` 无 save/resolve/commit、切换文件三选一、`?conflictScenario=damaged|binary|truncated|missing` 四场景 fallback 警告与出口草稿保留、720×480 小高度顶部导航/角色条/块 X/Y/草稿区可达且主体 ScrollArea 滚动归属、严格 CSP `PRODUCTION_EQUIVALENT_CSP` 零违规且 `adoptedStyleSheets>0`、块导航键盘可达焦点不丢）；同步修复 `src/webview/features/diff/cspCompatObserver.ts`（垫片扩 `insertBefore`/`append`/`Element.insertBefore` 插入路径，document.head 与 `data-theme-css`/`data-core-css`/`data-unsafe-css` `<style>` 统一转 `adoptedStyleSheets`）、`src/webview/styles/global.css`（小高度 `.conflict-workspace` 固定 320px/max-45vh 滚动归属）、`src/webview/features/conflicts/ConflictDiffView.svelte`（0 块已解决不误报 fallback） | `tests/webview-e2e/conflict-v011-f.spec.ts` 6/6、`tests/webview-e2e/workbench.spec.ts`/`chinese-scroll.spec.ts`/`page-screenshots.spec.ts`（V011-D details 折叠回归修复：显式点击 需要帮助 展开） | v0.1.1（开发中） | 开发中 | 规划 `releases/v0.1.1` |
| AI 完全关闭主路径 E2E（v0.1.1 收口） | `tests/webview-e2e/conflict-ai-disabled.spec.ts`（退出条件「主路径在 AI 未配置时完整可用」：`?ai=disabled` mock 未配置外部模型，验证进入冲突页基础信息无 AI 报错、三种结果仅 `conflict/draft-update` 无 AI 请求与 Host 写操作、帮助区展示本地建议且如实标注「本地检查」不标为模型、草稿三选一/保存/导出可用、全程无 console 错误级 AI 噪音）；`src/webview/mocks/mockWorkbench.ts` `conflictSnapshot` 支持 `?ai=disabled`/`?conflictAi=disabled`，model 置「本地规则（未配置外部模型）」触发本地建议分支 | `tests/webview-e2e/conflict-ai-disabled.spec.ts` 5/5 | v0.1.1（开发中） | 开发中 | 规划 `releases/v0.1.1` |
| 统一冲突差异视图受控三动作（v0.1.1 批次 C） | 领域 `src/conflict/conflictResolution.ts`（纯函数 `applyConflictResolution(text, conflictIndex, resolution)` 按 `parseConflictRegions` 位置替换对应块，`current→mine/incoming→theirs/both→mine+theirs（先我的后对方，Tooltip 已说明另一顺序延期 v0.1.2）`，损坏/越界 fail-closed 返回 `ConflictParseError`，`isStaleConflictAction/isStaleByText` 基于 `ConflictFileIdentity+ContentHash(hashText)` 过期校验）；`src/webview/features/conflicts/ConflictDiffView.svelte`（受控 `controlledText` 状态，`$effect` 同步外部 `workingText` 重置，`parseConflictRegions` 统一块计数，`handleAction` 先 `isStale` 中文提示“内容已过期，请刷新后重试”再 `applyConflictResolution` 更新草稿并保持视口在同一块附近，`getControlledResult` 返回受控草稿，`staleMessage` 警告区 `role=alert`，`fileIdentity/expectedHash/newHash+conflict` enriched payload 上报，`theme 动态重建延期至 V011-D（需监听主题消息重建实例）`，绝不发起 Host 写操作） | `tests/unit/conflictResolution.test.ts`（三动作各自结果/both 顺序/多块连续/过期拒绝/损坏越界/CRLF/无BASE/超长行/payload 完整性）、`tests/components/ConflictDiffView.test.ts`（V011-C：三动作更新受控结果并上报 enriched fileIdentity/expectedHash/newHash、Git/多块/CRLF/无BASE/超长行挂载、损坏 fail-closed、过期由领域层判定）、`tests/unit/conflictDiffViewAdapter.test.ts`（顶层 vi.mock 消除警告） | v0.1.1（开发中） | 开发中 | 规划 `releases/v0.1.1` |
| 可编辑合并结果与可撤销取舍（v0.1.2） | `src/conflict/mergeDocumentModel.ts`（合并文档模型）、`src/webview/features/conflicts/conflictResultEditorAdapter.ts`、`src/webview/features/conflicts/ConflictResultEditor.svelte`、`src/webview/features/conflicts/MergeActionToolbar.svelte`、`src/webview/features/conflicts/conflictShortcuts.ts` | `tests/unit/mergeDocumentModel.test.ts`、`tests/unit/conflictResultEditorAdapter.test.ts`、`tests/components/ConflictResultEditor.test.ts`、`tests/components/MergeActionToolbar.test.ts`、`tests/webview-e2e/conflict-v012.spec.ts` | v0.1.2（开发中） | 开发中 | 规划 `releases/v0.1.2` |
| 冲突保存核验与 Resolve 闭环（v0.1.3） | `src/conflict/conflictCompletionModel.ts`（11 态状态机+8 失效传播+非文本分支+deriveStepView）、`src/conflict/conflictSaveService.ts`（保存原子写入+token 契约+完整复验链）、`src/conflict/conflictVerification.ts`（7 项确定性核验+人工确认边界）、`src/conflict/conflictRecovery.ts`（12 类错误三段式+非文本出口）、`src/webview/features/conflicts/ConflictStepBar.svelte`（五阶段步骤条）、`src/extension/workbench/WorkbenchController.ts`（save-working 原子化/Resolve 前置核验+SVN 状态复验）、`src/extension/workbench/workbenchSession.ts`、`src/webview/mocks/mockWorkbench.ts`、`src/webview/i18n/terminology.ts` | `tests/unit/conflictCompletionModel.test.ts`（39）、`tests/unit/conflictSaveService.test.ts`（20）、`tests/unit/conflictVerification.test.ts`（27）、`tests/unit/conflictRecovery.test.ts`（37）、`tests/unit/conflictActivitySafety.test.ts`、`tests/components/ConflictStepBar.test.ts`、`tests/components/ConflictsModuleV013E.test.ts`、`tests/webview-e2e/conflict-v013.spec.ts`（4） | v0.1.3（开发中） | 开发中 | 规划 `releases/v0.1.3` |
| Diff 窗口、协议 v2 与原生编辑器对比 | `src/extension.ts`、`src/extension/workbench/workbenchWindowManager.ts`、`src/extension/workbench/workbenchRouting.ts`、`src/extension/workbench/WorkbenchController.ts`、`src/extension/workbench/nativeDiffContentProvider.ts`、`src/protocol/workbenchProtocol.ts`、`src/webview/app/workbenchState.svelte.ts`、`src/webview/features/diff/DiffModule.svelte`、`package.json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `tests/unit/workbenchRouting.test.ts`、`tests/unit/workbenchWindowManager.test.ts`、`nativeDiffContentProvider.test.ts`、`extensionManifest.test.ts`、`workbenchProtocol.test.ts`、`workbenchState.test.ts`、`tests/components/DiffModule.test.ts`、Extension Host 与人工候选验收                                                                                                                                                                                                                                                                                                                                                                                                                              | v0.0.4（Diff 独立窗口）；v0.0.5（统一模块窗口） | 已发布（v0.0.4、v0.0.5）               | `releases/v0.0.4`、`releases/v0.0.5` |
| AI 配置、本地检查/影响引擎与收敛（v0.0.12 批次 C 后） | 本地引擎 `src/ai/changeIntelligence.ts`（`buildLocalChangeReview`/`buildLocalImpactAnalysis`，领域契约 `AiReviewSnapshot`/`ImpactSnapshot`）供变更解读本地适配；按目录/文件类型分组 `src/ai/commitSplitAi.ts`、`src/webview/features/changelists/`；旧 `ai-review`/`impact`/`agent` 模块已移除；AI 配置 `src/ai/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 单元测试、Webview                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | v0.0.1；v0.0.9 收敛命名                 | 已实现，本地降级保留                   | `releases/v0.0.1`；规划 `releases/v0.0.9` |
| 提交说明建议草稿保护（v0.0.9 §4） | 纯逻辑 `src/commit/commitMessageSuggestion.ts`（`insertSuggestionBlankFields`、`replaceDraftWithSuggestion`、`diffDraftAgainstSuggestion`、`COMMIT_MESSAGE_MAX_LENGTH`）；协议 `CommitMessageSuggestion` 与 `CommitSnapshot.messageSuggestion`、`commit/adopt-suggestion`/`commit/undo-suggestion-replace`/`commit/discard-suggestion`（`src/protocol/workbenchProtocol.ts`）；Host `WorkbenchController`（generate 只生成建议不覆盖草稿、adopt 采用前重新校验候选与范围、undo 恢复备份、discard、快照 stale 标记与持续撤销反馈、`isCommitMessageSuggestionStale`、commit 成功后清空）；会话 `workbenchSession.ts`（`CommitSessionState.messageSuggestion`/`messageSuggestionReplaceBackup`）；项目切换守卫 `projectSwitchGuard.ts`（`hasCommitMessageSuggestion`）；Webview `CommitModule.svelte`（建议区、行级差异、插入空白字段/替换/复制/放弃、替换前字符数确认、撤销入口）；Mock `src/webview/mocks/mockWorkbench.ts` | `tests/unit/commitMessageSuggestion.test.ts`（纯逻辑全分支）、`tests/unit/workbenchCommitMessageSuggestion.test.ts`（Controller 级：不覆盖、插入/替换/撤销/放弃、token 失效、过期拒绝、无输入、手动编辑失效备份）、`tests/components/CommitModule.test.ts`（建议区展示、差异、插入/替换/复制/放弃、过期只读、撤销入口、占位标记）、`tests/unit/projectSwitchGuard.test.ts`、`tests/unit/workbenchCommitSelectionGate.test.ts`（旧 generate-message 断言按新契约更新）、`tests/unit/workbenchProtocol.test.ts` | v0.0.9（规划） | 开发中，保护契约已落地 | `releases/v0.0.9` |
| 受限差异外发回执与有证据的提交说明（v0.0.11） | 纯逻辑 `src/commit/commitDiffEvidence.ts`（`AnalysisReceipt`/`DiffCoverageState`/`DiffFileCoverage`/`EvidenceReference`/`ClaimStatus`/`CommitMessageClaim`/`ValidatedCommitMessageClaim`/`validateCommitMessageClaims`/`hashText`/`buildCandidateId`/`scanSensitiveContent`/`splitDiffHunks`/`buildCommitDiffFragment`/`applyDiffBudget`/`validateEvidenceReferences`/`isCommitDraftEvidenceStale`）；IO 薄层 `src/commit/commitDiffCollector.ts`（`collectLimitedCommitDiffs`/`readWorkingCopyRevision`，预算 `COMMIT_DIFF_PER_FILE_BUDGET`=6000、`COMMIT_DIFF_TOTAL_BUDGET`=40000）；协议 `CommitReceiptView`/`CommitDiffFileCoverageView`/`commit/receipt`、`commit/preview-receipt`/`commit/receipt-dismiss`/`commit/open-evidence`/`commit/retry-failed-diff`、`CommitMessageSuggestion.claims`（`src/protocol/workbenchProtocol.ts`）；AI 请求 `AiCommitMessageRequest.diffMode/receipt/coverage/diffs` 与结果 `AiCommitMessageResult.evidence`/`claims`（`AiCommitMessageClaim`，`src/ai/aiProvider.ts`）、生成器 `src/ai/commitMessageAiGenerator.ts`（受限差异本地回退带证据与逐条声明、`normalizeCommitMessageResult` 严格证据/声明结构校验）；Host `WorkbenchController`（preview-receipt 只采集/脱敏/裁剪下发回执不调用模型、generate-message 携带 `receiptToken` 确认后生成、`validateEvidenceReferences` 证据校验、`validateCommitMessageClaims` 声明校验与强制降级、建议带 coverage/coverageFiles/evidence/claims/receipt/revision 绑定、`open-evidence` 复验 token/时效/候选/范围后路由 Diff、`retry-failed-diff` 只重采失败项并下发新回执、`isCommitMessageSuggestionStale` 纳入 revision、候选变化清除旧回执）；会话 `workbenchSession.ts`（`pendingReceipt`/`workingCopyRevision`）；Host 辅助 `workbenchSupport.ts`（`resolveWorkingCopyRevision`）；Webview `CommitModule.svelte`（生成输入模式、回执展示与确认、coverage/coverageFiles 与证据展示、逐条声明状态/降级/证据、打开差异、重试失败项）、`workbenchState.svelte.ts`（`commit/receipt` 一次性状态）；Mock `src/webview/mocks/mockWorkbench.ts` | `tests/unit/commitDiffEvidence.test.ts`（纯逻辑全分支，含声明校验降级）、`tests/unit/workbenchCommitReceipt.test.ts`（Controller 级：preview-receipt 不下发模型、token 不匹配/候选变化拒绝、dismiss、limited-diff 建议带证据/claims/coverage/receipt/revision、metadata-only 无 claims、adopt 前复验、open-evidence 路由与拒绝、retry-failed-diff 重试/无失败项/非 limited-diff 拒绝）、`tests/components/CommitModule.test.ts`（回执展示/展开/确认/降级/放弃、生成输入模式、证据与覆盖率展示、逐条声明状态/降级/证据、打开差异、重试失败项、过期禁用）、`tests/webview-e2e/workbench.spec.ts`（受限差异回执 / 打开证据 / 重试失败项 / 逐条声明）、`tests/unit/workbenchProtocol.test.ts` | v0.0.11（规划） | 开发中 | 规划 `releases/v0.0.11` |
| 变更解读统一闭环（v0.0.12 批次 A） | 领域层 `src/understanding/`（`changeUnderstanding.ts` 统一 Snapshot/状态机/合并/会话内确认待复核/时效/草稿建议、`understandingLocal.ts` 复用 `changeIntelligence`、`understandingAi.ts` 请求与严格校验、`understandingCollector.ts` 复用 `commitDiffCollector` + `AnalysisReceipt.task=understand-changes`）；协议 `understanding`/`understanding/analyze`、`ChangeUnderstandingSnapshot`、动作 run-local/preview-receipt/receipt-dismiss/run-model/open-evidence/retry-failed/confirm-fact/clear-confirmations、消息 `understanding/receipt`（独立不改动 `commit/receipt`）；Host `WorkbenchController`（动作路由 + composeUnderstandingSnapshot + 跨任务拒绝 + open-evidence 复验 + retry-failed）、`workbenchSession.ts`（understandingState）、`projectSwitchGuard.ts`、`workbenchPresentation.ts`；Webview `UnderstandingModule.svelte`、`workbenchState.svelte.ts`、`FeatureRouter.svelte`、`i18n/terminology.ts`；入口 `svnWorkbench.understandScope` + extension.ts；Mock `mockWorkbench.ts`、`navigation.ts` | `tests/unit/changeUnderstanding.test.ts`、`tests/unit/workbenchUnderstanding.test.ts`（Controller 级：run-local 不调用模型、回执/跨任务拒绝/确认待复核/open-evidence/retry-failed）、`tests/components/UnderstandingModule.test.ts`、`tests/webview-e2e/workbench.spec.ts`（主闭环/证据打开）、`src/test/suite/index.ts`（AI 场景清单） | v0.0.12（规划） | 开发中 | 规划 `releases/v0.0.12` |
| 提交说明接入有效确认 + 按改动意图拆分（v0.0.12 批次 B） | 跨模块共享 `src/extension/workbench/understandingConfirmations.ts`（项目键隔离、候选 hash 一致的仍有效确认，仅会话内）；`commit/generate-message` 只使用仍有效确认（`AiCommitMessageRequest.userConfirmations`、`CommitMessageSuggestion.userConfirmations`）；`commitSplitAi.ts`（`AiCommitSplitRequest.userConfirmations/diffs`、`AiCommitSplitSuggestion.purpose/dependencies`、`createLocalCommitSplitResult` 无差异时明确降级为目录/文件类型分组、`normalizeCommitSplitResult` 透传、`validateCommitSplitResult` 范围/去重）；协议 `changelist/preview-receipt`/`receipt-dismiss`/`run-semantic`、消息 `changelist/receipt`（`ChangelistReceiptView`，任务 changelist-split 独立回执，跨任务拒绝）、`AnalysisReceipt.task` 增 `changelist-split`；Host `WorkbenchController`（`previewChangelistSplitReceipt` 只下发回执、`run-semantic` 校验任务/token/范围/候选后语义拆分、`preview-apply` 执行前复验重复归属 fail-closed）；Webview `ChangelistsModule.svelte`（语义拆分入口、回执三动作、purpose/依赖展示）、`CommitModule.svelte`（“已使用 N 条变更解读确认”提示）、`UnderstandingModule.svelte`（“按改动意图拆分”入口）、`workbenchState.svelte.ts`（`changelist/receipt` 一次性状态）；Mock `mockWorkbench.ts` | `tests/unit/commitSplitSemantic.test.ts`（降级/语义/透传/去重）、`tests/unit/workbenchChangelistSemantic.test.ts`（Controller 级：回执/跨任务拒绝/重复文件阻止/确认事实有效与过期排除）、`tests/components/ChangelistsSemantic.test.ts`、`tests/webview-e2e/workbench.spec.ts`（语义拆分主闭环/确认事实提示） | v0.0.12（规划） | 开发中 | 规划 `releases/v0.0.12` |
| 冲突意图解释（v0.0.12 批次 C） | 领域 `src/ai/conflictInterpretation.ts`（§7 六段 `AiConflictInterpretation`、`buildConflictInterpretationRequest` 复用受限文本预算、`createLocalConflictInterpretation` 本地回退如实声明无法判断、`normalizeConflictInterpretation` 严格校验）；`AiProvider.interpretConflict` + OpenAiCompatibleProvider（复用 conflictAdvice 场景，不启用 conflictMerge）；协议 `conflict/preview-receipt`/`receipt-dismiss`/`interpret`、消息 `conflict/receipt`（`ConflictReceiptView`，任务 conflict-interpret 独立回执，跨任务拒绝）、`ConflictSnapshot.interpretation`；`AnalysisReceipt.task` 增 `conflict-interpret`；Host `WorkbenchController`（preview-receipt 只读冲突正文算预算、interpret 校验任务/token/scope/冲突集 hash 后调用模型并绑定、冲突集/revision 变化标 stale 只读、receipt-dismiss）；Webview `ConflictsModule.svelte`（解释入口、回执三动作、六段展示、验证命令仅 <code> 展示不执行）、`workbenchState.svelte.ts`（`conflict/receipt` 一次性状态）；Mock `mockWorkbench.ts`；保存与 Resolve 契约不变 | `tests/unit/conflictInterpretation.test.ts`（六段/标记检测/严格校验）、`tests/unit/workbenchConflictInterpret.test.ts`（Controller 级：回执/跨任务拒绝/冲突集变化 stale/dismiss）、`tests/components/ConflictsModule.test.ts`（六段展示/回执确认/命令仅展示）、`tests/webview-e2e/workbench.spec.ts`（冲突意图解释主闭环） | v0.0.12（规划） | 开发中 | 规划 `releases/v0.0.12` |
| Svelte Shell 与模块路由             | `src/webview/`、`src/extension/workbench/`、`src/protocol/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Webview、协议单元测试                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | v0.0.1；v0.0.2 CSP 增强                         | 开发中                                 | `releases/v0.0.2/变更映射.md`        |
| 工作台 Host 职责边界                | `workbenchSession.ts`、`workbenchPresentation.ts`、`workbenchFileOperations.ts`、`workbenchSupport.ts`、`repositoryWorkbenchActions.ts`、`workbenchWindowManager.ts`（按模块窗口管理）、`workbenchRouting.ts`（统一模块路由与防御）、`workbenchWindowRegistry.ts`（按模块单例注册表）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `tests/unit/workbenchArchitecture.test.ts`、`tests/unit/workbenchRouting.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | v0.0.2                                          | 开发中                                 | `releases/v0.0.2/变更映射.md`        |
| 多窗口安全上下文与失效广播          | `src/security/svnSecurityContextRegistry.ts`（按 repository identity 引用计数共享认证与证书信任、合并同步、显式清除与失效事件；对未持有引用的重复 release 为安全 no-op）、`src/security/svnSecurityContext.ts`；控制器会话生命周期接线 `src/extension/workbench/WorkbenchController.ts`（一控制器最多持有一个仓库安全引用：首次会话 acquire、同仓库重开不重复 acquire、换仓库 release 旧/acquire 新、关窗或 dispose 恰好 release 一次；异步凭据刷新在写回前复验会话仍为当前会话）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `tests/unit/svnSecurityContextRegistry.test.ts`、`tests/unit/svnSecurityContext.test.ts`、`tests/unit/workbenchControllerSecurity.test.ts`（控制器生命周期回归：同仓库重复 open 不泄漏、双窗口共享、关闭交错不重复释放、异步读取期间会话切换/关闭不写回）、Extension Host                                                                                                                                                                                                                                                                                                                                                                                                                                      | v0.0.5                                          | 已发布（v0.0.5）                       | `releases/v0.0.5`                    |
| 首次上手与异常可恢复（v0.0.15） | 诊断动作协议化 `src/diagnostics/environmentDiagnostics.ts`（`DiagnosticActionId/Action` + `actions`）、`src/protocol/workbenchProtocol.ts`（`DiagnosticAction` + `DiagnosticsSnapshot.checks[].actions` + 5 诊断动作）、领域执行器 `src/diagnostics/diagnosticActions.ts`（仅写 `svnWorkbench.svn.path`、打开设置/文件夹、复制诊断、打开 URL）、`WorkbenchController` 仅编排（`diagnostics/*` 分发后原地重检）、`DiagnosticsModule.svelte`（可点击动作、首屏四状态收敛、验收清单 `import.meta.env.DEV` 显隐）、`src/extension.ts` `getSvnPath` 三选一（选择文件/打开设置/重新检测）、`README.md` 与 `package.json` 命令统一 | `tests/unit/environmentDiagnostics.test.ts`（四状态 + 动作）、`tests/components/DiagnosticsModule.test.ts`（可点击动作、首屏四状态、验收清单隐藏）、`tests/webview-e2e/workbench.spec.ts`（诊断动作原地重检）、`tests/unit/workbenchProtocol.test.ts` | v0.0.15                                         | 开发中                                 | `releases/v0.0.15`                   |
| 页内编辑与安全写入（v0.0.6）        | `src/diffEdit/diffPathGuard.ts`（lstat/realpath/scope 复验、拒绝 symlink/junction/目录/设备、≤5 MB、严格 UTF-8）、`diffEditTokenRegistry.ts`（editToken 单次使用/TTL/强绑定/撤销）、`diffAtomicWriter.ts`（按路径互斥、同目录临时文件、保留权限/BOM/EOL/末尾换行、写句柄 fsync 落盘——Windows 只读句柄 fsync 会确定性 EACCES、原子 rename、失败清理）、`diffDraftService.ts`（仅内存草稿、cleanContent 脏判定、draftRevision 拒绝重放/乱序、容量上限、导出补丁）、`diffEditingService.ts`（openEdit/saveWorking 编排：消耗 token→绑定校验→路径守卫→脏 TextDocument 拒绝→expectedContentHash/磁盘复验→原子写入→签发新 token）；Host 接线 `src/extension/workbench/diffEditHost.ts` 与 `WorkbenchController` 协议路由（diff/open-edit、diff/save-working、diff/draft-checkpoint、diff/draft-abandon、diff/draft-export；会话替换/面板销毁撤销 token）；Webview `DiffModule.svelte`/`DiffView.svelte`（审阅/编辑切换、Ctrl/Cmd+S、差异导航、逐块采用、脏状态、草稿恢复/放弃/导出、脏草稿目标切换三选一阻断对话框、保存拒绝后“重新建立编辑会话”恢复）、`diffHunks.ts`；保存后权威快照刷新生命周期：`App.svelte`（已有快照刷新保持模块挂载、仅显示“正在刷新当前范围…”提示条，不卸载编辑器）、`src/webview/features/diff/diffViewLifecycle.ts`（重建决策纯函数：挂载键/实际容器身份/old-new-patch 逐字段比较，避免大文本拼接碰撞）、DiffView 手动生命周期（编辑态同键同容器保持 FileDiff/Editor 实例不打断输入；容器身份/挂载键/只读态内容变化才重建；disposeAll 清理实际挂载的旧容器避免 DOM 残留）、DiffModule 保存结果按消息对象身份只消费一次（lastProcessedSaveResult）；生产 CSP 适配 `cspCompatObserver.ts`（插入前拦截垫片：style 属性改写、标记 `<style>` 转 adoptedStyleSheets；观察器兜底与可达性修正）；外部变化失效 `diffEditHost.ts` `watchDiffEditTargets`；三选一决定授权绑定与确认守卫 `diffTargetSwitch.ts`（save 的 targetId 必须等于挂起确认目标；脏草稿/活动会话才确认）；SVN 绑定探测 `diffSvnBinding.ts`（打开与每次保存复验 wc-root/repos-uuid/BASE hash 与 file external 双信号——status file-external 属性 + 父目录 svn:externals 目标名 `parseSvnExternalsTargetNames`，拒绝嵌套 WC、目录/文件 externals、UUID/BASE 变化）；协议 `DiffSnapshot.edit/draft`、`DiffSaveWorkingResult` 与拒绝原因、`diff/target-switch-confirm`/`diff/target-switch-decision`（协议 v2 扩展） | `tests/unit/diffEditingService.test.ts`（含 saveDraft、干净草稿不写盘、草稿不初始化为 BASE、5 MB 内容上限且 token 失效、路径撤销、真实 documentVersion 绑定）、`tests/unit/diffTargetSwitch.test.ts`（决定授权与确认守卫）、Extension Host `testDiffEditSvnBindingIsolation`（真实隔离 SVN fixture：嵌套 WC/externals/BASE 变化拒绝）、`tests/unit/diffHunks.test.ts`、`tests/components/DiffModule.test.ts`（编辑、连续保存 hash、恢复动作、三选一对话框用例，及编辑态快照刷新不重建/save-result 一次性消费回归）、`tests/components/AppLifecycle.test.ts`（快照刷新保持模块挂载）、`tests/unit/diffViewLifecycle.test.ts`（重建决策全分支：编辑态保持、只读态重建、挂载键/容器切换、逐字段碰撞）、`tests/components/harness/DiffModuleHarness.svelte`（controller 原地更新 props 不重挂载的回归 harness）、`tests/webview-e2e/workbench.spec.ts`（编辑+Ctrl+S、三选一切换、连续保存轮换 token/hash，repeat-each≥15）、`tests/webview-e2e/diff-edit-csp.spec.ts`（生产等价严格 CSP 零违规）、`tests/spike`（edit-mode Spike go，复用生产垫片）、Extension Host `testDiffEditIntegration`           | v0.0.6                                          | 已发布（v0.0.6）                       | `releases/v0.0.6`                    |
| 中文体验、滚动与可访问性            | `src/webview/i18n/`、`ScrollArea.svelte`、各功能模块                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `tests/webview-e2e/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | v0.0.1；持续增强                                | 持续门禁                               | 各版本 artifacts                     |
| 跨模块列表与路径一致性（v0.0.10）  | 共享层 `src/webview/components/list/`（`SearchInput.svelte`、`ResultCount.svelte`、`PreviewPathList.svelte`、`useFileList.svelte.ts` 行控制器，`listModel.ts` 泛化 `matchesFileQuery`/`FileQuerySource`、`PathCell.svelte` 泛化输入）；迁移模块 Changelists/History/Conflicts/BrowseTask/PropertiesTask/SelectionTask/Diagnostics/AiReview/Impact/Diff（详见 §8.7）；协议 `ChangelistGroupFileView`、`ConflictSnapshot.progress`；Host `buildChangelistsSnapshot` 候选富化、`buildConflictSnapshot` 进度基线 | `tests/components/ListShared.test.ts`、`ChangelistsModule.test.ts`、`HistoryModule.test.ts`、`ConflictsModule.test.ts`、`RepositoryModule.test.ts`、`PreviewPathList.test.ts`、`DiagnosticsModule.test.ts`、`IntelligenceModules.test.ts`、`SettingsModule.test.ts`、既有 `ListSelection.test.ts` 回归 | v0.0.10（开发中） | 开发中 | 规划 `releases/v0.0.10` |
| 认证、证书和凭据安全                | `src/security/`、`src/diagnostics/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 单元测试、Extension Host、恢复页 E2E                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | v0.0.1                                          | 已实现                                 | `releases/v0.0.1`                    |
| 深水区与打磨（v0.0.18）             | 新手引导 `src/webview/app/onboarding.svelte.ts` / `OnboardingStrip.svelte` 与 `svnWorkbench.openGuide`；状态解释 `StatusExplanation.svelte` + `i18n/terminology.ts`；History `HistoryQueryView`/`history/load-more`、`src/history/svnHistory.ts` 的输入校验、受限 SVN 参数、过滤和 `AbortSignal`；`WorkbenchSession.historyState{historyLimit,historyQuery}` 与 `HistoryModule.svelte` 条件表单；变更解读 seen 进度和 ScopeBar 快捷事实/可展开路径。 | `tests/unit/onboarding.test.ts`、`tests/components/OnboardingStrip.test.ts`、`tests/components/StatusExplanation.test.ts`、`tests/components/HistoryModule.test.ts`、`tests/unit/domainIoAdapters.test.ts`、`tests/components/ScopeBar.test.ts`、`tests/components/UnderstandingModule.test.ts`、`tests/webview-e2e/workbench.spec.ts`、`tests/webview-e2e/chinese-scroll.spec.ts` SCR-03/SCR-04 | v0.0.18 | 已发布（v0.0.18） | `releases/v0.0.18` |
| 连续任务上下文纯模型（v0.1.4 V014-A） | 纯逻辑 `src/extension/workbench/taskContinuity.ts`（`TaskContinuityContext` 来源/选择/活动文件/视图偏好/滚动锚/草稿引用/Diff 目标/版本；`createContinuityContext`/`reduceOnNavigate` 只保持或缩小 scope/`restoreAgainstSnapshot` 合法交集与逐项移除原因/`resolveActiveFileFallback` 邻项回退/`invalidateContinuity`/`isStaleSnapshot`/`resolveScrollAnchor` 锚点为主像素为辅/`checkDirtyDraftGuard` 纯判定；不依赖 vscode；Host 接线见 V014-C1 行，Webview 消费延期 V014-C2）；会话类型挂载 `WorkbenchSession.taskContinuity` | `tests/unit/taskContinuity.test.ts`（创建/迁移/交集/移除原因/新文件不自动加入/邻项回退/失效/旧 sessionId/scroll anchor/脏草稿守卫/收缩与扩大拒绝） | v0.1.4（开发中） | 开发中（纯模型；Host 接线已由 V014-C1 完成） | 规划 `releases/v0.1.4` |

| Changes 唯一主操作（v0.1.4 V014-B） | `src/webview/features/changes/ChangesModule.svelte`（`primaryAction` 五态互斥派生：clean/ready/conflicts-only/blocked/suggest/select-committable 兜底；数量口径均为整快照权威集合；冲突 CTA 与空状态按钮按态升降 primary；BulkActionBar 删除“检查当前范围并提交”并增设“更多”菜单（只灌选择不打开 Commit）；blocked 原因展开区；无协议/Host 变更） | `tests/components/ChangesPrimaryAction.test.ts`（五态唯一主操作/数量/更多只灌选择/blocked/冲突主次/权威计数/旧双 CTA 移除）、`tests/components/ListSelection.test.ts`（excluded 阻止语义同步 V014-B） | v0.1.4（开发中） | 开发中 | 规划 `releases/v0.1.4` |
| 连续上下文 Host 接线与协议字段（v0.1.4 V014-C1，不含 Webview 消费） | 协议 `src/protocol/workbenchProtocol.ts`（`ChangesSnapshot.continuityRestore?: ContinuityRestoreView` 可选向后兼容：contextVersion/originModule/changesView/合法交集 selectedKeys/activeFileKey/scrollAnchorKey/scrollAssistPixels/保守 commitDraft/removedEntries/notices/restoredAt；守卫 `isContinuityRestoreView`/`isChangesSnapshot`）；接线 helper `src/extension/workbench/taskContinuityWiring.ts`（`continuityResolveKey` 与视图 selectionKey 同源/`migrateContinuityForReopen` 重建迁移 scope 只缩不扩 + 项目切换丢弃 + 新会话重锚/`noteContinuityEvent`/`buildContinuityRestore` 合法交集 + 邻项回退 + 滚动锚 + 草稿保守）；`WorkbenchController` 接线点（open-diff 前 `captureContinuityForDiff` 快照源 Changes 会话、`open` 重建迁移、`buildSnapshot`/`sendChangesSnapshot` 同批候选装配并一次性消费、筛选/选择变更标记失效、窗口关闭丢弃；Diff 单例/targetKey 判等/四元校验/脏草稿三选一契约不变）；Mock `src/webview/mocks/mockWorkbench.ts`（`mockContinuityRestore` + `?continuity=restore`，默认不携带） | `tests/unit/taskContinuityWiring.test.ts`（守卫接受/拒绝、交集过滤、stale 忽略、失效清空、草稿保守、跨仓库剔除、迁移/扩大拒绝、open-diff 写入与转发、重建迁移与快照恢复、一次性消费、扩大丢弃） | v0.1.4（开发中） | 开发中（Host 侧；Webview 消费延期 V014-C2） | 规划 `releases/v0.1.4` |

| Changes → Commit 交接 Host 整批复验与协议（v0.1.4 V014-E 上半棒，不含 Commit UI 显示） | 协议 `src/protocol/workbenchProtocol.ts`（`CommitSnapshot.handoff?: CommitHandoffView` 可选向后兼容：source 固定 changes/selectionVersion/requestedCount/keptCount/removedEntries{path,reason,message}/receivedAt，只带相对路径；守卫 `isCommitHandoffView`/`isCommitSnapshot`，非法 handoff 整快照拒绝；Mock 经 `commitSnapshot` overrides 透传，类型同源无需改动）；纯逻辑 `src/commit/commitHandoff.ts`（`COMMIT_HANDOFF_SELECTION_VERSION`/`buildCommitHandoff` 整批复验 accepted/shrunk/rejected/`createCommitHandoffView`/`isStaleCommitHandoff`/`selectCommitHandoffForSnapshot`）；会话 `WorkbenchSession.commitState.handoff`（交接进入写入，手动改选/规则/AI 接管/冲突切换/版本过期清除，message 草稿独立保留）；`WorkbenchController` 接线点（`applyCommitHandoffSelection` 目标打开时落点：复用 `commitState.candidates` 缓存或同批采集，全拒恢复旧会话并中文拒绝且不建面板，收缩写入合法交集 + 旧 preview/token 失效 + 一次性 feedback，绝不写 manualSelectedPaths；`open()` 跨窗口与同窗 `open-module` 共用入口；`buildCommitSnapshot` 挂载 handoff 并丢弃过期版本，快照过滤移除已选项时旧 preview 失效 + 冲突收缩给出“处理冲突”指引；`commit/update-selection` 手动改选与规则/AI 接管清除 handoff） | `tests/unit/commitHandoff.test.ts`（整批复验全拒/收缩/移除原因/去重/只缩不扩/跨仓库不合并/守卫缺省合法与非法拒绝/stale 忽略）、`tests/unit/workbenchCommitHandoff.test.ts`（跨窗口 open 收缩与 handoff 快照/全拒恢复/空交接默认行为、同窗 open-module 收缩与全拒、手动改选失效草稿保留、冲突切换 preview 失效与指引、旧版本忽略、跨仓库 open 全拒与计划层越界阻止） | v0.1.4（开发中） | 开发中（Host 侧；Commit UI 显示属 E2） | 规划 `releases/v0.1.4` |
| Changes → Commit 交接 Commit UI 显示与冲突指引（v0.1.4 V014-E 下半棒） | `src/webview/features/commit/CommitModule.svelte`（紧凑模式摘要条 `role=region「待提交文件摘要」` 内交接来源行 secondary 展示：`handoff` 经 `isCommitHandoffView` 过滤、非法按无交接处理；source 为 changes 时显示“来自本地修改，范围未扩大”+“已带入 keptCount 个文件（共请求 requestedCount 个）”；removedEntries 逐条 message 经 `role=status` 播报、reason 文字标签 + 图标分组（不只靠颜色）；feedback 含“请先到冲突模块处理冲突”时摘要区给出“处理冲突”次级入口（`open-module {moduleId: conflicts, taskId: conflicts/resolve}`，button--secondary，唯一主操作仍只有一个 primary）；旧 preview 由既有 usablePreview 置空、不渲染旧预览主操作；无 handoff 时回到常态；紧凑四要素与折叠区结构不变）；Mock `src/webview/mocks/mockWorkbench.ts`（`mockCommitHandoffPayload` + `?commitHandoff=basic\|shrunk\|conflict` 演示载荷：全合法/收缩中文移除原因/冲突指引 + preview 置空，默认不携带） | `tests/components/CommitModuleHandoff.test.ts`（来源行与带入数量/收缩数量与逐条播报 + 原因标签/非法守卫按无交接/无 handoff 常态/冲突入口 action 与旧预览空态 + 唯一 primary/无指引不显示入口）、`tests/webview-e2e/commit-handoff.spec.ts`（`?commitHandoff=shrunk` Changes→Commit 来源行 + 移除播报 + 折叠区收起不重复控制台 + 唯一主操作；`?commitHandoff=conflict` 处理冲突入口直达冲突模块 + 无确认提交旧主操作） | v0.1.4（开发中） | 开发中 | 规划 `releases/v0.1.4` |
| Changes ↔ Diff 往返 Webview 消费与 Diff 返回入口（v0.1.4 V014-C2） | `src/webview/features/changes/ChangesModule.svelte`（`consumedRestorePayload` 非响应式一次性消费标记 + `suppressResetForRestore` 当次跳顶 + `isContinuityRestoreView` 校验 fail-closed；选择交集回填/活动行 setActiveRow + markActive/滚动锚优先像素仅钳制且 tick 后重放/视图有值回填缺省保持/query + activeStatus + activeFileType + activePresetId + sort 解析 + density + onlySelected/草稿第二道保守/removedEntries + notices 经 SelectionSummary role=status 播报；值不缓存不复用）；`src/webview/features/diff/DiffModule.svelte`（工具栏“返回本地修改”button--secondary → `open-module {moduleId: changes, taskId: changes/overview}`，不新建 Rail） | `tests/components/ChangesContinuityRestore.test.ts`（交集/逐条播报/非法不半应用/草稿回填与保守/视图回填与缺省保持/一次性消费/活动行命中与锚回退）、`tests/components/DiffModule.test.ts`（返回按钮存在与正确 action）、`tests/webview-e2e/continuity-restore.spec.ts`（`?continuity=restore` 全路径：恢复选择/活动行/草稿/播报 → Diff → 返回 → 再次恢复） | v0.1.4（开发中） | 开发中 | 规划 `releases/v0.1.4` |

<!-- prettier-ignore-end -->

### 8.1 跨平台路径与测试文件系统边界

- 绝对路径的 Map/Set 键、相等判断和父子范围判断统一使用
  `src/scope/pathIdentity.ts`。所有纯路径 API 要求显式 `PathSemantics`
  （`platform + cwd` 均必填，无默认回退）：领域函数不得自行读取
  `process.platform/process.cwd()`，生产 Host 从唯一边界
  `src/scope/nativePathSemantics.ts` 注入；合成 posix/win32 路径的测试在
  任意开发平台（包括 Windows）得到确定语义，平台问题不会拖到 CI 才暴露。
- `normalizePathIdentity` 只生成内部身份键；界面展示和真实文件操作必须继续
  使用原始绝对路径，不得把 Windows 小写身份键显示给用户。
- 身份/展示类型边界（v0.0.8）：`src/scope/pathBrands.ts`（零依赖，品牌归属
  身份领域；协议层经 type-only import 引用，scope 不反向依赖 protocol）
  定义互不兼容的 `PathIdentityKey` 与 `DisplayPath` 品牌；
  `normalizePathIdentity`、`workingCopyId`、`projectId`、`scmProjectKey`、
  `createScopedFileKey` 只返回 `PathIdentityKey`（用于 Map/Set、比较、排序、
  缓存键与范围判断）。协议展示字段（`WorkbenchScopeView` 的
  `projectWorkingCopyRelativePath` 与 `roots[].relativePath`、
  `WorkbenchFileView.projectRelativePath`、`file/path-detail-result` 的
  `projectRelativePath/workingCopyRelativePath/repositoryRelativePath/absolutePath`）
  声明为 `DisplayPath`；Host 构建处（`workbenchPresentation.toScopeView`、
  `workbenchFileOperations.withProjectFileView`、
  `WorkbenchController.respondFilePathDetail`）经唯一显式转换
  `toDisplayPath` 进入协议。`toDisplayPath` 参数类型 `DisplayPathSource`
  在类型层面拒绝 `PathIdentityKey`（直接传入 identity 键编译失败，由
  `npm run check` 的 tsc 与 svelte-check 强制，不是源码扫描提醒）。
- 编译期品牌契约（`src/scope/pathBrands.ts`）：`Assert<T extends true>` +
  `PathIdentityNotDisplayPath`/`DisplayPathNotPathIdentity`（品牌互斥，
  任一方向可互相赋值即编译失败）与 `DisplayPathRejectsIdentityKey`
  （`DisplayPathSource<PathIdentityKey>` 必须折叠为 never）；品牌或参数
  约束一旦退化，`npm run check` 必失败。
- 静态边界契约 `tests/unit/pathIdentityBoundary.test.ts` 扫描源码：协议展示
  字段必须是 `DisplayPath`、Webview 运行时目录不得导入 pathIdentity /
  pathBrands / nativePathSemantics（identity 键无法在 Webview 侧产生或转换）、
  领域纯函数无默认语义回退且不读取 process、唯一 native 边界集中读取。
- `isSameOrDescendantPath` 使用平台对应的 `path.relative` 判断边界，禁止用
  字符串前缀判断目录归属；`/repo/a` 不得覆盖 `/repo/ab`，名为 `..cache`
  的合法子目录不得被误拒绝。
- 页内编辑临时文件统一经 `writeAndSyncTempFile` 使用写句柄执行
  `write -> fsync -> close`；可注入的 open 契约用于在非 Windows 环境锁定
  `FlushFileBuffers` 需要写访问的行为。
- Extension Host/真实 SVN 测试目录统一经
  `src/test/suite/testTempDirectory.ts` 清理。仅 Windows 的
  `EPERM | EBUSY | ENOTEMPTY` 在有限重试后可延迟给临时 Runner 回收；其他
  平台或错误码必须继续失败。
- 主要回归位于 `tests/unit/windowsPlatformContracts.test.ts`、
  `tests/unit/dualPlatformPathContracts.test.ts`（v0.0.8：大小写保留与
  identity 等价、盘符、UNC、斜杠、中文、同前缀兄弟目录、项目相对路径、
  SCM 同名标题与切片，全部显式注入 posix/win32）、
  `tests/unit/pathIdentityBoundary.test.ts`、
  `tests/unit/extensionHostTempCleanup.test.ts` 与
  `tests/unit/githubWorkflow.test.ts`。
- 快速双平台契约门禁 `npm run test:platform-contracts`（v0.0.8）：上述可注入
  用例组成一套合成契约，任意开发平台均可运行；已纳入本地 `npm run verify`
  与 `.github/workflows/verify.yml`（Linux 快速门禁，先于完整覆盖率），同一套
  用例不在 Windows Runner 重复执行（Windows 保留完整覆盖率与真实 SVN 验收
  作为最终确认）。`npm run test:windows-contracts` 保留为同集合别名。
- 以上内容登记为 [`v0.0.7`](../releases/v0.0.7/) 第一批开发基础；它只统一
  既有绝对路径与平台契约，不代表 workspace container、project、working
  copy、repository 和 operation scope 的组合 identity 已经完成。

### 8.2 项目边界与活动项目解析（v0.0.7 批次 2）

- 领域类型与组合 identity：`src/scope/projectIdentity.ts` 提供
  `WorkspaceFolderRef`、`ProjectIdentity`、`WorkingCopyIdentity`、
  `RepositoryIdentity`、`createScopedFileKey` 与 `projectRelativePath`；
  identity 键只用于比较，Host 文件 key 组合 working copy identity 与规范
  化工作副本内路径，不得只使用 `projectRelativePath`。
- 活动项目解析契约：`src/scope/projectResolver.ts` 按固定顺序确定命令目
  标（命令 URI / Explorer 选择 → 活动编辑器最具体 folder → 容器保存的项
  目根 → 单根 folder → 多根选择器候选）；`finalizeProjectRoot` 在工作副
  本根确定后定案项目根，候选缺失或越界时回退工作副本根并标记
  `projectRootIsFallback`。业务入口禁止固定使用 `workspaceFolders[0]`。
- Host 接线：`src/extension.ts` 的 `resolveCommandTarget` 统一接入命令入
  口，多根无目标时打开 QuickPick 项目选择器（可搜索、键盘可用，突出最近
  项目但不自动进入）；项目根与最近项目仅保存在当前 workspace 容器的
  `workspaceState`（`svnWorkbench.projectRoot`、`svnWorkbench.recentProjectRoot`）；
  目标不在工作区项目中时明确提示。
- 范围与失效绑定：`OperationScope.project`（`src/scope/operationScope.ts`）
  经 `toScopeView` 映射为 `WorkbenchScopeView.projectName/
projectRootIsFallback/projectWorkingCopyRelativePath`；`hashOperationScope`
  纳入项目根，项目边界变化使旧快照、确认令牌与 AI 结果失效。
- 范围栏显示：`src/webview/components/svn/ScopeBar.svelte` 以项目名为主显
  示、工作副本名为次级，回退时提示“尚未设置项目根”。
- Diagnostics 归属分类：`src/scope/workingCopyClassification.ts` 区分独立
  工作副本根、上层工作副本、嵌套工作副本、external、非 SVN 与路径不存在；
  `WorkbenchController.classifyFolderWorkingCopyBinding` 复用工作副本解析
  器并在 SVN 可用时读取父目录 `svn:externals` 区分 external；
  `environmentDiagnostics.ts` 的工作区检查按归属汇总展示。
- 主要回归位于 `tests/unit/projectIdentity.test.ts`、
  `tests/unit/projectResolver.test.ts`、
  `tests/unit/workingCopyClassification.test.ts`、
  `tests/components/ScopeBar.test.ts` 与
  `tests/unit/workbenchArchitecture.test.ts`（范围视图项目上下文与失效哈希）。
- SCM 项目切片、项目总览页、跨项目选择与模块切换草稿守卫已在批次 3
  完成，见本文件 §8.3 与 [`v0.0.7`](../releases/v0.0.7/)。

### 8.3 路径显示、SCM 切片、项目总览、切换守卫与团队规则项目层（v0.0.7 批次 3）

- 文件主路径默认项目内路径：`withProjectFileView`（
  `src/extension/workbench/workbenchFileOperations.ts`）为 Changes/Commit 文件
  视图补充 `projectRelativePath`（显示）与 `projectName`（仅跨项目 scope 的
  徽标）；写操作身份仍是工作副本内 `relativePath`。路径详情经
  `file/path-detail` 按需由 Host 计算（项目内路径/工作副本内路径/仓库内路
  径/SVN URL/本地完整路径），范围外拒绝；SVN URL 由工作副本根检出 URL +
  逐段编码相对路径推导（`src/svn/svnUrl.ts`），禁止 repos-root 拼接；本地完
  整路径复制经 `file/copy-path` 由 Host 完成；`WorkbenchScopeView` 不暴露工
  作副本根或仓库 URL。Webview 侧共享组件
  `src/webview/components/svn/FilePathDetail.svelte`。
- SCM 项目切片：`src/scm/projectSlicing.ts`（标题消歧、按工作副本分组、按
  项目根切片、最具体项目归属）+ `src/scm/svnSourceControlManager.ts`（每个
  显式 workspace folder 一个项目级 provider；同一工作副本共享一次采集；未
  加载兄弟目录不进入项目 provider；未被 folder 覆盖的工作副本保留独立
  provider）。
- 项目总览与切换：新模块 `projects`（任务 `projects/overview`，
  `ProjectsSnapshot`/`ProjectOverviewItem`）；Host
  `WorkbenchController.buildProjectsSnapshot/openProjectTask/switchActiveProject`；
  Webview `src/webview/features/projects/ProjectsModule.svelte`；范围栏“切换
  项目”入口经 QuickPick，不静默切换；命令 `svnWorkbench.openProjects`。
- 跨项目选择拆分（§7.2）：`src/extension.ts` `prepareWorkbenchRequest` 逐目
  标解析——非 SVN 路径排除并说明；同工作副本明确跨项目多选允许并设置
  `OperationScope.projects`（文件徽标 + 提交预览按项目分组，CommitModule
  `previewGroups`）；跨工作副本/跨仓库拆分执行单元由用户选择，不合并修订。
- 项目切换草稿守卫（§8）：`src/extension/workbench/projectSwitchGuard.ts`
  （未完成内容检查与三选一决定）、`projectDraftStore.ts`（按 projectId +
  moduleId + scopeHash 隔离的草稿存储，容量上限淘汰最旧）；
  `WorkbenchController.open()` 在项目切换时拦截，恢复时重新采集候选复验手
  动选择（失效路径剔除、采集失败清空旧选择），会话替换即撤销旧编辑令牌，
  旧预览与确认 token 永不恢复；`hashOperationScope` 纳入项目根与排序后的
  跨项目项目集合。
- 团队规则项目层（§9）：`resolveSvnWorkbenchConfigLocation/
resolveSvnWorkbenchConfigWriteRoot`（`src/config/svnWorkbenchConfig.ts`）；
  读取继承与来源标记（`commitConvention.ts`、
  `commitSelectionRuleService.getEffectiveRules(repositoryRoot, projectRoot?)`，
  缓存键为工作副本根 + 项目根组合）；保存/新建默认写项目根；迁移预览与执行
  （`src/config/teamConfigMigration.ts` + `settings/preview-team-migration`/
  `settings/execute-team-migration`），只迁移白名单键，执行前复验源哈希、目
  标存在性与项目边界；事务执行层
  `src/config/teamConfigMigrationExecutor.ts`（IO 可注入）：排他创建目标防
  TOCTOU、同目录原子替换源、复验失败时先恢复源再按内容核对删除目标，补
  偿失败返回结构化 partial 与人工恢复步骤；设置页显示来源（当前项目/继
  承自工作副本根）。
- 主要回归位于 `tests/unit/projectFileView.test.ts`、
  `tests/unit/svnUrl.test.ts`、
  `tests/unit/workbenchPathDetail.test.ts`（Host 范围复验）、
  `tests/unit/scmProjectSlicing.test.ts`、
  `tests/unit/svnSourceControlManager.test.ts`（含同名 provider 动态消歧）、
  `tests/unit/projectSwitchGuard.test.ts`、
  `tests/unit/workbenchProjectSwitch.test.ts`、
  `tests/unit/teamConfigProjectLayer.test.ts`、
  `tests/unit/teamConfigMigrationExecutor.test.ts`、
  `tests/components/FilePathDetail.test.ts`、
  `tests/components/ProjectsModule.test.ts` 与
  `tests/components/SettingsTeamMigration.test.ts`。

### 8.4 选择内核（v0.0.8 批次 1，纯逻辑）

- 无 DOM/VS Code/Svelte 依赖的 TypeScript 领域模块 `src/selection/`，
  契约见 [`v0.0.8`](../releases/v0.0.8/) §4/§5 与 UX08-SEL-01/02/03/04/06、
  UX08-SORT-02；不修改 Svelte、WorkbenchController 与协议（集成在后续批次）。
- 选择集合与三态 `src/selection/selectionCore.ts`：`SelectionKey` 复用批次 0
  的 `PathIdentityKey` 品牌（普通 string/DisplayPath 编译期不可直接赋值，
  编译期 Assert 强制与 DisplayPath 互不兼容）；模块不把 DisplayPath 当
  identity、不读取 process。actionability 是针对当前动作的调用方权威输入
  （`SelectableItem.actionable` 必填），模块只做 fail-closed 运算：可操作项
  = actionable=true 且非 blocked（blocked 是二次 fail-closed；Commit 调用方
  对 excluded 传 actionable=false，允许的非提交动作可传 true；needsReview
  可由调用方明确 actionable=true 且保持标记）。三态 none/partial/all 只基于
  当前筛选可操作项；表头 toggle 只影响传入快照的可操作 key（隐藏选择保留、
  新出现项不因过去全选自动加入）；blocked 永不被批量加入；
  `mergeRecommendedSelection` 只加不减（推荐且可操作、非 excluded/needsReview
  项加入，不覆盖用户手动保留）；隐藏选择计数/集合、clear hidden、
  only-selected 视图过滤。
- 自然稳定排序 `src/selection/selectionSort.ts`：路径/文件名/归属大小写
  不敏感 numeric natural compare（file2 < file10，逐字符扫描、前导零不改变
  数值），相等以原始输入位置稳定兜底；状态与选择建议经显式产品优先级表
  （`RECOMMENDATION_ORDER` = recommended < needsReview < excluded < blocked），
  未知值恒排末尾、降序只反转已定义值；`toDefaultOrder` 恢复调用方原始顺序。
- 刷新合法交集 `src/selection/selectionRefresh.ts`：以调用方复合稳定 key
  保留 selected ∩ 刷新后保留项；消失/越界/blocked 由调用方以 retained +
  removalReason 判定（模块不猜测、不放宽安全边界）；同一 identity 在刷新
  快照中重复出现是数据完整性异常，fail-closed 取消选择并返回确定原因
  （“刷新快照存在重复身份，已安全取消选择”），与顺序/retained 组合无关；
  新文件绝不自动加入；返回新集合（selected + removed 明细，无冗余字段），
  旧集合不变。
- 主要回归位于 `tests/unit/selectionCore.test.ts`、
  `tests/unit/selectionSort.test.ts`、`tests/unit/selectionRefresh.test.ts`
  （含 5,000 项数据集、输入不变异、稳定性与未知值契约）。
- `src/selection/**/*.ts` 已纳入 `vitest.config.mts` 的 coverage include，
  受全局 80% 阈值约束（当前 statements/branches/functions/lines 均 100%）。

### 8.5 共享列表底座与 Changes/Commit 集成（v0.0.8 批次 2）

- 选择身份：`WorkbenchFileView.selectionKey`（协议 type-only 复用
  `PathIdentityKey`）由 Host 经 `createScopedFileKey`（working-copy identity
  - 规范化仓库内路径）在 `buildWorkbenchFileViews` 与提交/变更集快照中生成；
    无法建立身份 fail-closed 排除并记录。Webview 只能接收、携带与比较，不展
    示、不复制、不作为 SVN/fs 参数；动作仍提交 relativePath 由 Host 复验。
- 选择适配层 `src/webview/app/fileSelection.ts`：`toSelectableItems`（按动作
  权威 actionable：blocked 永不可操作；excluded 不进批量，Changes 允许逐项
  明确选择、Commit 不可选）、`buildKeyPathMap`/`buildPathKeyMap` 查表、
  `cloneSelection`；没有任何第二套三态/排序/刷新算法。
- 共享列表底座 `src/webview/components/list/`：`PathCell.svelte`（文件名 +
  项目内父目录两行、中部省略、徽标、读屏全名、详情按钮三端可达）、
  `SortHeader.svelte`（role=columnheader + aria-sort + 图标 + 中文方向）、
  `SelectionSummary.svelte`（已选/可操作/隐藏 + 只看已选/清除隐藏/清空全部/
  推荐项 + role=status 播报）、`BulkActionBar.svelte`（列表内 Sticky 底栏）、
  `listModel.ts`（状态优先级、筛选匹配、中部省略、键盘导航纯函数）。
- 列表偏好 `src/webview/app/listPreferences.ts`：排序/密度按模块经
  Webview state 本地保存，不发送 Host、不跨模块串用。
- Changes/Commit 集成：快照更新经 `refreshSelectionSet` 保留合法交集并播
  报移除原因；Commit 回声防护（未回显前旧快照不覆盖用户选择）与选择变化
  即撤销旧预览可用性；规则/AI 更新选择反馈明细
  `src/commit/selectionChangeSummary.ts`；Commit 确认提交/预览按钮显示实际
  数量。
- 键盘语义：Ctrl/⌘+A 幂等选择当前筛选可操作项；PageUp/PageDown 分页导航
  活动行（无活动行保留原生滚动）；Shift+F10 / Menu 打开 Changes 活动行菜单；
  Escape 先于空列表早退关闭路径详情并恢复触发点焦点；Shift+方向键连续选择；
  IME/输入框不劫持。
- Host 提交选择 fail-closed：`src/commit/commitSelectionValidation.ts`
  （去重 + 候选复验 + 旧状态清理），`WorkbenchController.applyWebviewSelection`
  为 update-selection / preview / generate-message 的统一校验入口；
  `CommitSessionState.candidates` 缓存权威候选，`manualSelectedPaths` 只跟踪
  用户手动选择；`buildCommitSnapshot` 一次性建立可生成 selectionKey 的候选集，
  无法建立身份者同步排除，保持 files/summary/选择一致。
- 主要回归位于 `tests/unit/listModel.test.ts`、
  `tests/unit/fileSelection.test.ts`、
  `tests/unit/commitSelectionValidation.test.ts`、
  `tests/unit/workbenchCommitSelectionGate.test.ts`、
  `tests/components/ListSelection.test.ts`、
  `tests/components/FilePathDetail.test.ts`、
  `tests/webview-e2e/list-operations.spec.ts`（UX08-SEL/SORT/FLOW/A11Y/VIEW/
  PERF，含 dataset=seven 批量语义与 Sticky 底栏遮挡）与
  `tests/webview-e2e/visual-accessibility.spec.ts`（三主题 × 三尺寸 +
  reduced motion）。

### 8.6 人工验收环境（v0.0.8 候选）

- `scripts/create-manual-acceptance-env.js`（`npm run prepare:manual-test-env`）
  生成可重复的人工验收环境：两个独立 SVN 仓库/工作副本（repo/repo2、
  wc/wc2，含远端模拟 remote-wc/remote-wc2）、多根
  `svn-workbench-manual-acceptance.code-workspace`、wc1/vendor/external-lib
  经 `svn:externals` 归属第二仓库、第一列文本状态 M 恰好 7 个的批量选择
  素材（OrderList、中文/空格/# 路径、空文本、>5MB、二进制、同名
  a|b/README.md）、长中文/空格/# 路径、conflict/blocked、真实刷新失效
  素材（WC2 `src/lib/stale.ts` 本地 M + 远端删除，`svn update` 后成为
  tree-conflict，旧选择刷新被移除）；输出一页式
  `README-manual-acceptance.md`（17 项真人步骤 + 通过/失败记录位，不含
  “人工已通过”结论）。安全：删除保护 fail-closed——validationRoot 只允许
  `path.resolve(os.tmpdir(), 固定目录名)` 的 exact match（Windows 额外允许
  既有默认盘根固定路径的 exact match），拒绝 HOME/工作区/任意父目录下
  同名 basename；只对本地 file:// fixture 初始化提交；生成后自检
  （WC1 M 集合 === 7 个、externals URL 语义精确指向 repo2/trunk、WC2 stale 更新前
  M 且远端有 `*` 标记），任一不满足即抛错。纯函数与 `svn status --xml`
  解析经 `require.main === module` 守卫可被测试 require；解析器在 Windows
  `target\\...` 边界把路径分隔符规范为 `/`，POSIX 合法反斜杠文件名保持原样；
  URL 比较只归一化 `%7E` 等 unreserved 编码，`%2F` 等 reserved 边界保持严格。
- 主要回归：`tests/unit/manualAcceptanceEnv.test.ts`（安全根 exact match
  与危险路径拒绝、`parseStatusXmlModifiedPaths`、workspace 双 folder、
  仓库/WC 分离、external 与 7 modified 数据、清单不虚构人工通过）。

### 8.7 跨模块列表与路径一致性（v0.0.10）

- 共享底座固化：`src/webview/components/list/useFileList.svelte.ts` 把
  Changes/Commit 原复制级重复的键盘导航（IME 闸门、Escape 先于空列表早退
  关闭路径详情并恢复触发点焦点、Ctrl/⌘+A、PageUp/PageDown 分页、Shift
  连续选择、Shift+F10/Menu 行菜单）、活动行焦点、窗口化（`windowedRows`）
  与路径详情开合收敛为单一控制器；模块经 options 省略不适用能力（选择、
  行菜单），但不得改变共享手势语义。新增 `SearchInput.svelte`（清除按钮、
  IME 安全）、`ResultCount.svelte`（role=status 结果数量）、
  `PreviewPathList.svelte`（写操作预览路径清单：搜索、复制清单、逐条复制
  与路径详情；不可勾选改范围）。`listModel.ts` 的 `matchesFileQuery` 泛化
  为 `FileQuerySource`（状态可选），`PathCell` 输入同步泛化。Changes/Commit
  重构为消费共享实现，行为由 `tests/components/ListSelection.test.ts` 回归
  守护；共享层语义测试 `tests/components/ListShared.test.ts`（含
  `tests/components/harness/ListHarness.svelte`）。
- Changelists：协议 `ChangelistsSnapshot.groups[].files`（`ChangelistGroupFileView`，
  Host 用候选富化；候选缺失或无身份键的文件仍展示、不可选择）替代裸
  `paths: string[]`；模块提供变更集与未分组的搜索（路径/状态）、路径/状态
  排序（自然排序 + 产品优先级表，偏好经 `listPreferences` 按模块保存）、
  手动多选 + Shift 连续选择 + 幂等“选择当前筛选”、批量“移出变更集
  （N）”（只提交已选且属于变更集的路径）/“加入应用栏（N）”/复制已选
  路径，均走既有 `changelist/preview-apply` → `execute-apply` 令牌流程；
  分组折叠保留筛选匹配数量；AI/本地建议保持有意顺序不排序。
- History：修订列表最新/最早排序 + 搜索清除 + 结果数量 + 键盘导航；比较
  固定 2 条（淘汰最早选择）并提供清空，不提供全选；Changed Paths 支持
  路径搜索、操作类型筛选（选项从数据推导）与路径/操作排序，行内复制与
  路径详情（仓库路径范围外时 Host 如实拒绝并显示中文原因）；恢复预览
  目标提供复制与路径详情。
- Conflicts：`ConflictSnapshot.progress`（会话首次冲突总数、剩余、已处理，
  `conflictState.initialCount` 基线，选择失效重置保留基线）；路径搜索 +
  冲突类型/产生操作筛选 + 路径/类型/来源排序 + 上一个/下一个未解决冲突
  导航（role=status 播报）；行内路径详情与仓库定位（复用
  `file/path-detail`、`file/copy-path`、`changes/show-in-repository`）；
  不提供批量 Resolve。
- Repository Browser：面包屑区分 SVN 仓库根（`info.repositoryRoot`）与
  项目根（`info.url` 检出地址）并以文字徽标标记；名称筛选、类型/名称/
  修订/作者/日期/大小排序（`formatZhFileSize`）、目录优先切换；目录行
  打开下级、文件行复制 URL；上级导航与复制当前 URL 保留。Properties：
  属性名搜索、名称排序、复制属性名/值/完整目标路径（`file/copy-path`），
  编辑保持单项预览。Update/高级操作/Shelf 预览清单接入 `PreviewPathList`。
- Settings/Diagnostics：规则预览支持路径搜索、决策/状态/来源筛选（选项
  从预览数据推导）与稳定排序，排序不改变规则优先级（上移/下移仍是唯一
  途径）；Diagnostics 提供“只看需要处理”、状态筛选、结果数量与复制单项
  （状态 + 详情 + 建议动作），状态图标带文字标签。
- 过渡复用（只读，完整合并页留给 v0.0.12）：AiReview 标题/证据/路径搜索
  与严重度/分类筛选；Impact 风险/标题排序、区域与路径搜索、路径详情；
  Diff 工具栏复用复制路径/路径详情/仓库定位（`DiffSnapshot.relativePath`，
  FeatureRouter 透传 `pathDetail`）。两者均不增加复选框或批量动作。
- 路径详情接入范围：FeatureRouter 向 changes/commit/changelists/history/
  conflicts/repository（含任务透传）/impact/diff 传递 `pathDetail`；
  `PreviewPathList` 与各模块的详情浮层关闭后恢复触发按钮焦点。
  `FilePathDetail` 复制按钮补充含路径值的 `aria-label`，不再只依赖 title。

### 8.8 受限差异外发回执与有证据的提交说明（v0.0.11）

- 领域契约（`src/commit/commitDiffEvidence.ts`，纯逻辑）：动作级回执
  `AnalysisReceipt`（任务 `commit-draft`、项目、模型、数据类型、文件数、
  字符总预算、单文件预算、是否含历史）；逐文件覆盖率 `DiffCoverageState`
  五种状态（`analyzed`/`truncated`/`binary`/`readFailed`/
  `budgetExcluded`）与 `DiffCoverageSummary`；敏感信息扫描与脱敏
  `scanSensitiveContent`（API 密钥、令牌、密码、私钥、连接串，重叠命中
  合并为一次 `[已脱敏]`）；差异块身份 `splitDiffHunks`（hunkId =
  hash(candidateId + 头 + 块内容)）；逐文件片段 `buildCommitDiffFragment`
  （读取失败/二进制不外发、超单文件预算截断）；总预算 `applyDiffBudget`
  （超预算文件只记 coverage 不发正文）；证据引用校验 `validateEvidenceReferences`
  （候选/路径/hunk 三重校验，虚构、范围外、过期引用丢弃并返回中文原因）；
  时效 `isCommitDraftEvidenceStale`（scope/候选/revision 任一变化即过期）；
  §5 逐条声明 `validateCommitMessageClaims`（每条声明证据逐条校验，模型标
  confirmed 但无任何有效 Host 证据的声明强制降级为 toConfirm 并计降级数，
  inferred/toConfirm 不因有证据而升级，无效引用保留并附原因）。
- IO 薄层（`src/commit/commitDiffCollector.ts`）：按当前 `operationScope`
  对已选文件逐个 `svn diff --internal-diff`，读取原文交纯逻辑脱敏/裁剪/预算，
  并读取工作副本 revision（`readWorkingCopyRevision`）；任何单个文件失败
  如实进入 `readFailed` coverage，不静默吞掉。预算：单文件 6000 字符、
  总计 40000 字符（`COMMIT_DIFF_PER_FILE_BUDGET`/
  `COMMIT_DIFF_TOTAL_BUDGET`）。
- 协议：`CommitReceiptView`（token/回执/覆盖率/包含与排除文件清单/不会
  发送项/服务商保留策略提示）、`CommitDiffFileCoverageView`；消息
  `commit/receipt`；动作 `commit/preview-receipt`（只采集并下发回执，
  不调用模型）与 `commit/receipt-dismiss`（放弃回执）。
  `CommitMessageSuggestion` 增加 `diffMode`（`metadata-only`/
  `limited-diff`）、`coverage`、`evidence`（每条含 `valid` 与 `reason`）、
  `receipt`，`binding` 增加 `revision`。
- AI 契约：`AiCommitMessageRequest` 增加 `diffMode/receipt/coverage/diffs`
  （脱敏差异片段携带 candidateId/hunkId）；`AiCommitMessageResult` 增加
  `evidence` 与 `claims`（`AiCommitMessageClaim`：text + status
  confirmed/inferred/toConfirm + 可选 evidence）；`normalizeCommitMessageResult`
  对证据与逐条声明做严格结构校验，畸形条目丢弃并计入警告，引用/声明有效性由
  Host 经 `validateEvidenceReferences`/`validateCommitMessageClaims` 复验
  （Webview/模型不信任假设）。受限差异模式本地回退同样生成带证据的结构化
  说明与逐条声明（已分析文件 → confirmed 附证据；无法判断项 → toConfirm），
  明确标记“无法判断具体改动”而不编造。
- Host 接线（`WorkbenchController`）：`commit/preview-receipt` 采集受限
  差异→构建回执与覆盖率→存入 `CommitSessionState.pendingReceipt`→下发
  `commit/receipt`（此时未调用模型）；`commit/generate-message` 的
  `limited-diff` 必须携带匹配 `receiptToken` 且 scope/候选哈希未变化才调用
  模型，证据引用经 `validateEvidenceReferences` 校验后进入建议，无效引用
  丢弃并计入警告；建议携带 `coverage`/`coverageFiles`/`receipt`/`revision`
  绑定；`adopt` 前 `isCommitMessageSuggestionStale` 纳入 revision 复验；
  候选变化时同步清除旧 pendingReceipt（fail-closed）。生成/失败/超时/取消/
  降级/过期均不覆盖用户草稿（沿用 v0.0.9 §4）。
- Host 证据打开（§4/AI11-DRAFT-02）：`commit/open-evidence` 只接受建议中
  valid=true 的引用，先校验建议 token 与时效（scope/候选/revision），再按
  `buildCandidateId` 映射回当前候选并复验范围，最后路由到 Diff 窗口打开
  该文件差异；token 不匹配、引用无效、文件已不在候选或范围外、建议过期
  均拒绝。
- Host 部分完成重试（§6）：`commit/retry-failed-diff` 只对建议
  `coverageFiles` 中 `readFailed`/`budgetExcluded` 的失败项重新采集受限差异
  并下发新回执（`pendingReceipt.retryNote` 说明仅覆盖重试项），确认后经
  `commit/generate-message` 生成新建议替换旧建议（用户草稿不变）；无失败项、
  建议过期或非 limited-diff 建议均拒绝。
- Webview（`CommitModule.svelte`）：生成输入模式选择（仅文件信息 /
  含差异需确认）；回执面板展示任务、模型、数据类型、预算、覆盖率、包含/
  排除文件清单（可展开）、不会发送项与保留策略提示，提供“开始模型生成”
  “继续仅文件信息”“放弃”三动作；建议区展示差异覆盖率、逐文件覆盖情况
  （失败项可辨识）、证据引用（有效/无效原因 + 每条有效证据的“打开差异”
  入口）与“重试失败项（N）”；§5 逐条声明展示状态（已证实/推断/待确认）、
  降级标记与逐条证据（可打开差异）。`commit/receipt` 为一次性等待确认状态，
  任何快照/错误到达后由 `workbenchState.svelte.ts` 清除。
- 建议带 `claims` 逐条声明注解层（§5，不替代 message）：`CommitMessageSuggestion.claims`
  每条含 text/status（confirmed/inferred/toConfirm）/downgraded/evidence/
  invalidEvidence；Host 在生成时经 `validateCommitMessageClaims` 逐条校验并
  强制降级（confirmed 无有效证据 → toConfirm + 警告）；仅文件信息模式不生成
  claims。Webview 逐条展示状态、降级标记与逐条证据（可打开差异）。

### 8.9 变更解读统一闭环（v0.0.12 批次 A）

- 领域层 `src/understanding/`：`changeUnderstanding.ts` 提供统一
  `ChangeUnderstandingSnapshot`（state/binding/receipt/coverage/changes/
  findings/verification/userConfirmations/limitations/draftProposal）、
  `mergeUnderstandingResults`（来源逐条标记 local-rule/configured-model/user/
  mixed；本地硬阻止项始终在前、不被模型降级）、`buildUserConfirmedFact` 与
  `markConfirmationsNeedsReview`（会话内确认绑定候选 hash，变化标待复核、
  绝不静默沿用）、`isUnderstandingSnapshotStale`（scope/候选/revision 时效）、
  `buildDraftProposalFromConfirmations`（待复核确认不进草稿）；`understandingLocal.ts`
  复用 `changeIntelligence` 的本地检查/影响分析适配为 changes（结构性陈述，
  有差异证据才 confirmed）/findings（本地硬项）/verification；`understandingAi.ts`
  定义 `AiUnderstandingRequest/Result` 与严格结构校验、本地回退映射；
  `understandingCollector.ts` 复用 `commitDiffCollector` 采集受限差异并构建
  `AnalysisReceipt.task=understand-changes` 回执。
- 协议：`WorkbenchModuleId understanding`/`WorkbenchTaskId understanding/analyze`
  （defaultTasks/taskModules/moduleIds 三处清单）；`ChangeUnderstandingSnapshot`
  加入 `WorkbenchModuleSnapshot`；动作 `understanding/run-local`/`preview-receipt`/
  `receipt-dismiss`/`run-model`/`open-evidence`/`retry-failed`/`confirm-fact`/
  `clear-confirmations`；消息 `understanding/receipt`（`UnderstandingReceiptView`，
  独立于已发布 `commit/receipt`，不改动其 Webview 状态）；`AnalysisReceipt.task`
  扩为 `commit-draft|understand-changes`，Host 的 pending receipt/token 显式绑定
  任务，跨任务一律拒绝。
- Host：`WorkbenchController` 新增变更解读动作路由与 `composeUnderstandingSnapshot`
  （合并本地/模型/确认，stale 只读，反馈一次性）；run-local 不调用模型；
  preview-receipt 只下发回执；run-model 校验任务/token/scope/候选后调用模型，
  结果经 `validateEvidenceReferences` 复验、confirmed 无证据降级 toConfirm；
  open-evidence 复验后路由 Diff；retry-failed 只重采失败项；confirm-fact 写入
  会话内状态并绑定候选 hash。`workbenchSession.ts` 增加 `understandingState`；
  `projectSwitchGuard.ts` 把会话内确认计入切换守卫；`workbenchPresentation.ts`
  getModuleTitle 增加“变更解读”。
- Webview：`UnderstandingModule.svelte` 首屏用途/范围/来源/状态，回执三动作，
  改了什么/需要你确认/影响与验证/准备提交四段，证据卡片打开差异，会话内确认
  输入（IME 保护），过期只读；`workbenchState.svelte.ts` 增加 `understanding/receipt`
  一次性状态（独立于 commitReceipt）；`FeatureRouter.svelte` 分发；`i18n/terminology.ts`
  变更解读术语。入口：`svnWorkbench.understandScope` 命令 + Explorer `svnWorkbench.ai`
  子菜单 + `extension.ts openUnderstanding`。
- Mock/E2E：`mockWorkbench.ts` 变更解读快照/回执/动作；`tests/webview-e2e/navigation.ts`
  增加“变更解读: understanding”。

### 8.10 提交说明接入有效确认 + 按改动意图拆分（v0.0.12 批次 B）

- 跨模块会话内共享：`src/extension/workbench/understandingConfirmations.ts` 以项目键
  隔离存储变更解读的会话内确认（仅内存、不落盘），understanding 在组合快照时同步；
  commit 生成提交说明与 changelists 语义拆分读取时只取“候选 hash 一致且非待复核”
  的事实，任何不匹配 fail-closed 返回空（过期确认绝不进入模型）。
- 提交说明接入确认（复用 v0.0.11 契约）：`commit/generate-message` 读取仍有效确认
  并入 `AiCommitMessageRequest.userConfirmations`；`CommitMessageSuggestion.userConfirmations`
  如实展示“已使用 N 条变更解读确认事实”；不覆盖/采用/撤销/时效/证据校验契约不变。
- 按改动意图拆分（§6）：`changelist/preview-receipt` 只采集受限差异并下发回执
  （任务 changelist-split，脱敏/预算沿用 6000/40000）；`changelist/run-semantic`
  校验任务/token/scope/候选后携带差异 + 确认事实调用模型；`changelist/suggest`
  默认元数据分组并在无差异时明确降级（purpose 声明“按目录/文件类型分组”）；模型
  永不加入 scope 外文件（`validateCommitSplitResult` 范围/候选/去重）；`changelist/preview-apply`
  与 `execute-apply` 均复验“同文件不得进入两个实际 Changelist”（fail-closed）。
- 实际套用保留既有 preview→确认→写入链路（`changelist/execute-apply` 经 `applySvnChangelist`）；
  模型结果只生成建议，用户手动调整后经预览确认写入 SVN Changelist。

### 8.11 冲突意图解释（v0.0.12 批次 C）

- 领域：`src/ai/conflictInterpretation.ts` 定义 §7 六段 `AiConflictInterpretation`
  （我的/对方修改意图、共同点、冲突点、推荐处理方式及证据、无法判断的业务选择、
  保存后验证）；`buildConflictInterpretationRequest` 复用 `buildConflictAiRequest`
  的逐文件受限文本预算；`createLocalConflictInterpretation` 本地回退如实声明
  “本地无法读取双方业务意图”并列出待确认未知项；`normalizeConflictInterpretation`
  严格校验（推荐方式必须为合法枚举，畸形字段丢弃）。
- 模型：`AiProvider.interpretConflict`（复用 `conflictAdvice` 场景配置，不启用
  `conflictMerge`）；Prompt 约束只读、验证命令仅建议、业务未知必须如实列出。
- 协议：任务 `conflict-interpret` 加入 `AnalysisTask`；`conflict/preview-receipt`/
  `receipt-dismiss`/`interpret` 动作；`conflict/receipt` 消息（`ConflictReceiptView`
  含逐文件字符预算与截断/读取失败标记，独立于 commit/understanding/changelist
  回执）；`ConflictSnapshot.interpretation` 六段 + source/fallbackReason/binding/stale。
- Host：`conflict/preview-receipt` 只读取冲突正文并计算预算（不调用模型）；
  `conflict/interpret` 校验任务/token/scope/冲突集 hash 后调用模型并绑定
  （scopeHash + conflictHash + revision）；冲突集或 revision 变化后解释标 stale
  只读（只能查看，不进入保存/Resolve）；`conflict/receipt-dismiss` 放弃回执。
- Webview：冲突页“解释冲突意图”入口 + 回执三动作（开始解释/继续仅本地建议/放弃）+
  六段展示（验证命令以 <code> 仅展示不执行）；`workbenchState.conflictReceipt`
  一次性状态（独立于其他回执）。
- 保存与 Resolve 契约不变：解释只辅助用户编辑工作副本，保存（conflict/save-working
  token/hash）与 Resolve（preview-resolve 预览 → resolve 确认）原样保留。

### 8.12 会话状态总线——草稿不丢、选择不乱（v0.0.13）

- 草稿总线（批次 A）：`src/extension/workbench/projectDraftStore.ts` 泛化为通用总线——类型化 `DraftKind="commit"|"conflict-merge"|"patch"`、`CommitDraft/ConflictMergeDraft/PatchDraft/GenericDraft`、`ConflictFileDraft{content,updatedAt,revision,baseContent}`、`MAX_PROJECT_DRAFTS=32`/`MAX_CONFLICT_FILE_DRAFTS=32`，按 `projectId+moduleId+scopeHash`（`projectDraftKey` 归一化）隔离，容量最旧淘汰；纯领域函数 `writeConflictFileDraft/getConflictFileDraft/isConflictFileDirty/deleteConflictFileDraft/readConflictMergeDraft` 可单测，Controller 仅持有内存 `ProjectDraftMap` 实例（`conflictDraftStore`）不写 `workspaceState`、不跨重启，符合“草稿不持久化、不跨重启、保存检查点≠写入工作副本”。
- 冲突草稿接入（批次 B）：`src/protocol/workbenchProtocol.ts` 新增 `ConflictSnapshot.selected.draft`、`conflict/draft-checkpointed|draft-switch-confirm` 消息与 `conflict/draft-update|draft-checkpoint|draft-abandon|draft-copy|draft-export|draft-switch-decision` 动作（`webviewActions` 双向守卫同步）；`WorkbenchController` 仅编排——`conflictDraftStore` 内存持有，`get/set/clear/isDirty` 均经 store 纯函数，`buildConflictSnapshot` 注入 draft 且 `workingView` 不使用 `as never`（构造 `{content,truncated,readError}` 兼容对象），并将冲突路径集合与工作副本原始内容缓存至 `session.conflictState.conflictPaths/workingBaseContents`（快照重建时刷新）；`conflict/select|refresh|open` 的脏判断与 `conflict/draft-update|checkpoint`（逐键触发）均读缓存（`isConflictFileDirty` 已存 `baseContent`、`conflictPaths` 存在性校验），不为每次按键重采 `collectConflictItems/buildConflictAiRequest`（高频路径无 SVN），注释说明取舍；30 秒超时自动按 save 的安全默认在 Webview 三选一对话框文案中告知“30 秒未选择将自动保存检查点并继续（草稿不丢）”；`conflict/save-working` 的 `fs.writeFile` 包 `try/catch`，失败时 `editState.feedback` 内联展示、编辑器与草稿保留、不触发 Resolve；`conflict/draft-copy|export` 经 `vscode.env.clipboard` 逃生（`operation/result`）；`conflict/draft-switch-decision` 三选一（保存检查点/留在当前/放弃）键盘可达、焦点返回触发按钮、IME 组合键保护。`src/webview/app/workbenchState.svelte.ts` 新增 `conflictDraftAck/conflictSwitchRequest` 一次性状态。
- 会话级共享选择（批次 C）：`WorkbenchSession.selectedPaths` 升级为 Changes/Commit/Changelists/Patch 共读（`WorkbenchController.applyWebviewSelection/buildCommitSnapshot` 同步 `session.selectedPaths`，`commit/apply-local-rules|ai-select` 同步）；`buildChangelistsSnapshot` 消费 `session.selectedPaths` 经 `filterCommitSelectionByCandidates` 得 `preselected{count,paths}+preselectedFeedback`（筛选变化仅提示、不静默扩大，右键范围只能缩小）；`ChangelistsModule.svelte` 展示“已带入 N 个文件（来自会话共享选择，右键范围未扩大）”与列表首次 `selected.size===0` 时的自动同步（不覆盖用户已有选择）。
- 逃生与同步（批次 D）：冲突草稿 `conflict/draft-copy|export` 按钮禁用态基于 `draft.hasDraft`，成功走 `operation/result`；`src/webview/mocks/mockWorkbench.ts` 的 `injectHostMessage` 改为 `HostToWebviewMessage["type"]` 并新增 `changelists+preselected`、`conflict/draft-*` 与 `conflictSave=fail` 失败保留的模拟；`FeatureRouter/App` 透传新增状态。

### 8.13 通用操作意向单——危险操作确认一致性（v0.0.14 批次 A-E）

- 批次 A 通用意向单组件与可访问性：`src/operation/operationIntent.ts`（纯领域，定义 `OperationIntentKind`（commit/resolve/update/revert/delete/switch/cleanup/property/changelist-apply/file-operation）、`OperationIntentView{token,kind,title,summary,paths,scopeHash,candidateHash,revision,repositoryUuid,createdAt,canExecute,issues,commands,stale}`、`operationIntentTitle`（动作+数量，如“提交 3 个文件”“还原 2 个文件”，数量来自最终候选集合）、`buildOperationIntentSummary`、`isOperationIntentStale`（repositoryUuid/scopeHash/candidateHash/revision 任一变化即 stale）、`validateOperationIntentForExecute`（token 不匹配/stale/issues/canExecute 失败均给出中文原因））；`src/webview/components/operation/OperationIntentDialog.svelte`（通用对话框：意图摘要+影响清单可搜索/复制复用 `PreviewPathList` 底座 + 确认 token + 范围/revision 变化自动失效只读；可访问性一次做齐：`role=dialog`+`aria-modal`+`aria-label=title`，`showModal` 背景不可交互，打开时焦点进入首个主操作按钮，Esc 关闭触发取消且焦点返回触发按钮，Tab 焦点锁定循环，显式“取消”按钮，状态不只依赖颜色（文字+图标+notice 多通道），保留 IME composition 保护——候选阶段 Enter 不触发确认，用 `isImeComposing` + `oncompositionstart/end` 跟踪）；`src/webview/i18n/terminology` 复用中文术语（提交/还原/删除等）。`tests/setup.ts` 已有 `HTMLDialogElement` 的 `show/showModal/close` jsdom polyfill，组件测试无需额外垫片。
- 批次 B 凭证泛化（协议 + Host 复验）：`src/protocol/workbenchProtocol.ts` 导出 `OperationIntentView/OperationIntentKind`（与领域共用类型，经 `src/operation/operationIntent.ts` 单一来源，`WORKBENCH_PROTOCOL_VERSION` 仍为 2 的增量兼容）；`WorkbenchController` 引入 `validateOperationIntentForExecute`，在 `commit/execute` 等写操作执行前以通用校验器复验 token/范围/revision/candidateHash（数量来自最终候选集合），范围/revision 变化后旧意向单只读失效、不得凭旧 token 执行；参考 v0.0.11/0.0.12 的 `commit/preview-receipt → receipt → execute` 链，本版本的写操作预览→确认→执行前复验契约保持不变，不引入一键撤销承诺。
- 批次 C 验证模式（Commit 与 Resolve 先接入）：`src/webview/features/commit/CommitModule.svelte` 的“确认提交（N）”改为先打开 `OperationIntentDialog`（按 `usablePreview` 构造 commit 意向单：`title="提交 N 个文件"`、`summary` 含执行前复验说明、`paths=selectedPaths`、`stale=selectionOutOfSync`、`canExecute` 与 `issues/commands` 直传，数量来自 `preview.selectedPaths.length`），对话框确认后才 `commit/execute` 并携带 token；`src/webview/features/conflicts/ConflictsModule.svelte` 的“标记解决”同样经意向单确认（`title="标记解决 1 个冲突"`、`paths=[relativePath]`、`commands=[resolve 命令]`），与已有的 draft 三选一守卫共存（不冲突：三选一守卫在切换/刷新时，意向单在 Resolve 确认时）。两者均保留 IME 保护与焦点返回。
- 批次 D 推广接入（Update / Revert / Delete / Switch 等）：现有各写操作（`ChangesOperationPreview` 的 revert/delete、Repository 的 update/switch/property/cleanup/advanced、Changelist 的 apply）已具备 `token + candidateHash/scopeHash + canExecute + issues` 预览契约，本版本按批次 C 模式逐个接入 `OperationIntentDialog`（意图摘要写明动作与数量，数量来自各操作最终候选集合；执行前经 `validateOperationIntentForExecute` 复验）；Host 侧保持“执行前重新校验操作范围、候选状态、工作副本或 revision 状态以及确认令牌”，旧预览和旧 AI 结果在范围变化后失效（沿用 v0.0.13 选择总线的失效语义）。
- 批次 E Mock、类型守卫与测试同步：`src/webview/mocks/mockWorkbench.ts` 的 `commit/preview` 与 `conflict/preview-resolve` 等 Mock 预览保持 `token/canExecute/selectedPaths` 并可经意向单对话框确认；`src/protocol/workbenchProtocol.ts` 的 `webviewActions` 与 `HostToWebviewMessage` 类型守卫同步导出新增类型；新增 `src/operation/operationIntent.ts` 纯领域单测 `tests/unit/operationIntent.test.ts`（标题/摘要/stale/校验分支）、组件测试 `tests/components/OperationIntentDialog.test.ts`（可搜索/复制清单、失效只读、Esc/取消/确认 token、IME 保护）与校验器级参数化测试 `tests/unit/operationIntentHost.test.ts`（`validateOperationIntentForExecute` 按 8 操作×4 分支 + scope/stale 额外分支，共 32 用例，覆盖校验器本身；装配级覆盖待 `test:coverage` 检查 `WorkbenchController.ts` 与 `repositoryWorkbenchActions.ts` 新增校验装配块，未覆盖分支再补针对性测试），覆盖成功/拒绝/过期/失败/恢复分支；`docs/current/设计与交互基线.md` 同步 §8.6 意向单交互规范，`docs/releases/v0.0.14/README.md` 实施状态更新。

### 8.14 首次上手与异常可恢复（v0.0.15 批次 A-E）

- 批次 A 诊断动作协议化：`src/diagnostics/environmentDiagnostics.ts` 检查项从纯文案 `action?: string` 升级为携带可执行动作 `actions?: DiagnosticAction[]`（`DiagnosticActionId: selectSvnExecutable|openSettings|rerunDiagnostics|openFolder|copyDiagnostics|openUrl` + label + params）；`src/protocol/workbenchProtocol.ts` 同步新增 `DiagnosticAction`/`DiagnosticsSnapshot.checks[].actions` 与 5 个诊断动作 `diagnostics/select-svn-executable|open-settings|open-folder|copy-diagnostics|open-url`（`webviewActions` 双向守卫同步）；新增领域执行器 `src/diagnostics/diagnosticActions.ts`（仅写 `svnWorkbench.svn.path`、打开设置/文件夹、复制诊断、打开 URL，不自动修改系统，凭据不进 Webview）；`WorkbenchController` 仅编排（`diagnostics/*` 分发后 `sendDiagnosticsSnapshot` 原地重检）；`DiagnosticsModule.svelte` 渲染为可点击动作（主动作 `button--primary`，状态不只靠颜色，键盘可达，IME 保护，`role=region` + `aria-label`）。
- 批次 B `src/extension.ts` `getSvnPath` 失败路径改造：原纯警告文案改为可执行三选一 `showWarningMessage`（选择 SVN 可执行文件 / 打开设置 / 重新检测），选择文件经 `showOpenDialog` 写入 `svnWorkbench.svn.path`，打开设置经 `workbench.action.openSettings`，重新检测清除缓存后重试；返回仍为 `configured || "svn"` 保持调用方契约，错误信息满足“发生了什么、可能原因、恢复动作”三要素。
- 批次 C 首次四状态收敛：`DiagnosticsModule.svelte` 基于 `svn-cli` 与 `workspace` 检查结果派生 `firstRunState`（可用 / CLI 缺失 / 路径无效 / 非工作副本），各有主、次动作（查看修改 / 选择可执行文件 / 重新选择并检测 / 打开文件夹）+ 通用的重新检测与复制诊断入口；所有失败页均有原地重试与复制诊断入口。
- 批次 D C-11 人工验收清单入口对普通用户隐藏：`DiagnosticsModule.svelte` 以 `import.meta.env.DEV` 控制 `showAcceptance`（开发/测试环境 `DEV=true` 时显示验收清单 Tab，否则隐藏）；C-12 README 与 `package.json` 命令统一：`README.md` 常用命令删除重复的 `检查环境`，按 `package.json` 实际 28 个命令标题完整列出（含 `svnWorkbench.openWorkbench` 等），并标注验收清单仅开发/测试可见。
- 批次 E Mock、类型守卫、测试同步：`src/webview/mocks/mockWorkbench.ts` 诊断快照注入保持 `actions` 可经新对话框确认；`src/protocol/workbenchProtocol.ts` `webviewActions` 与 `HostToWebviewMessage` 同步；新增单测 `tests/unit/environmentDiagnostics.test.ts`（四状态动作协议化）、组件测试 `tests/components/DiagnosticsModule.test.ts`（可点击动作、首屏四状态、验收清单隐藏）与 E2E `tests/webview-e2e/workbench.spec.ts`（诊断动作点击后原地重检）；`docs/current/设计与交互基线.md` 按需同步，`docs/releases/v0.0.15/README.md` 实施状态更新。

### 8.15 操作时间线与有限恢复（v0.0.16 批次 A-E）

- 批次 A 操作记录领域模型与会话存储：新增 `src/activity/activityRecord.ts`（`ActivityRecordKind`/`ActivityNextAction`/`nonRecoverable` 标记 + `buildActivityNextActions`）、`src/activity/activityStore.ts`（`MAX_ACTIVITY_RECORDS=64`、`createActivityStore`/`appendActivityRecord`/`clear`，纯内存不写磁盘，会话结束清空）、`src/activity/snapshotFreshness.ts`（`SnapshotFreshness{capturedAt,scopeHash,revision}`、`isSnapshotStale`/`formatStaleMessage`/`createSnapshotFreshness`）；`WorkbenchController.activityStore` 仅内存持有，不入 `workspaceState`，API Key/凭据/证书私密材料不进记录，中文文案复用 `src/webview/i18n/`。
- 批次 B 三类状态接入时间线：草稿（`diff/draft-checkpoint`、`conflict/draft-checkpoint` 写入 `writeConflictFileDraft`/`checkpointDiffDraft` 成功后 `appendActivityRecord` kind=`draft-checkpoint`）、确认事实（`understanding/confirm-fact` 成功后 kind=`understanding-confirmation`）、意向单执行结果（`commit/execute` 成功/失败在 `executeCommit` 内 kind=`operation-execution`，result `success`/`failed`，nonRecoverable 按任务判定，失败携带 `errorReason`，成功携带 `revision`）；所有记录含时间/任务/项目/范围/影响数量/预览摘要/执行结果/错误原因，不承诺远端 commit/merge/switch/relocate 可一键撤销。
- 批次 C 时间线视图：`src/webview/features/activity/ActivityModule.svelte` 复用列表底座（SearchInput/ResultCount/ScrollArea、键盘可达、IME 保护、role/status 多通道），展示记录列表、错误内联（role=alert）、每条记录的可执行下一步（重试/查看冲突/打开日志/复制诊断，`activity/retry|view-conflicts|open-output|copy-diagnostics|view-history`），SVN 无安全恢复方式的操作标注`非可撤销`文案“此操作不能在工作台中一键撤销”；`src/protocol/workbenchProtocol.ts` 新增 `activity` 模块与 `activity/timeline` 任务、`ActivitySnapshot`、`activity/*` 6 个动作（`webviewActions` 双向守卫同步）、`moduleIds` 清单，`src/webview/app/FeatureRouter.svelte` 分发；Mock `src/webview/mocks/mockWorkbench.ts` 新增 `activitySnapshot` 工厂与 `open-module`/`refresh` 分发。
- 批次 D 快照新鲜度协议字段：`src/protocol/workbenchProtocol.ts` 新增 `SnapshotFreshness` 并为 `HistorySnapshot` 增加 `freshness?: SnapshotFreshness`，`WorkbenchController.buildHistorySnapshot` 以 `createSnapshotFreshness(session.scopeHash, session.workingCopyRevision)` 生成 `capturedAt`；`src/webview/features/history/HistoryModule.svelte` 基于 `freshness.capturedAt` 计算 `staleness`（≥5 分钟 stale，`scopeHash`/`revision` 变化亦 stale），过期时以 `notice--warning` + `codicon-warning` + 文字“此结果基于 N 分钟前的状态，工作副本已变化”提示，不只依赖颜色；History 等只读模块先行接入，恢复动作统一走 v0.0.14 意向单新预览 + 新确认，不复用旧 token。
- 批次 E Mock、类型守卫、测试同步：`src/protocol/workbenchProtocol.ts` 的 `webviewActions`/`HostToWebviewMessage` 同步；`src/webview/mocks/mockWorkbench.ts` `createInitialMockSnapshot`/`open-module`/`refresh` 纳入 `activity`；`WorkbenchController` 新增 `buildActivitySnapshot`/`sendActivitySnapshot`/`appendActivityRecord` 与 `activity/*` 动作路由（`refresh`/`retry`/`open-output`/`copy-diagnostics`/`view-conflicts`/`view-history`，retry 走新模块加载不复用旧 token）；`tests/unit/activityStore.test.ts`（容量上限、追加、过滤）、`tests/unit/snapshotFreshness.test.ts`（时间/范围/revision stale 分支）、`tests/components/ActivityModule.test.ts`（记录列表、错误内联、非可撤销标注、可执行下一步）、`tests/components/HistoryModule.test.ts`（新鲜度过期提示）、`tests/webview-e2e/workbench.spec.ts`（时间线与历史新鲜度）；`docs/releases/v0.0.16/README.md` §6 实施状态更新，设计与交互基线按需同步。

### 8.16 发现性与核心任务效率（v0.0.17 批次 A-F）

- 批次 A Update 独立模块：`src/protocol/workbenchProtocol.ts` 新增 moduleId `update`、任务 `update/preview`（`repository/update` 移除，旧深链安全失效）、动作 `update/preview`/`update/execute`（原 `repository/preview-update`/`repository/execute-update` 更名）、`UpdateSnapshot`（`kind/info/recovery?/preview?/result?/conflicts{count,paths,error?}`；`UpdatePreviewView`/`UpdateResultView` 自 `RepositorySnapshot` 拆出，后者不再含 update/lastResult 段）；Host 逻辑迁入 `src/extension/workbench/updateWorkbenchActions.ts`（`buildUpdateSnapshot` 重采当前范围冲突支撑常驻 CTA、`createUpdatePreview`、`executeUpdate` 保持意向单校验与执行前复验 token/范围/候选、活动记录 taskId 迁移），`WorkbenchSession.updateState{preview,candidateHash,result}` 替代原 repositoryState 的 update 段；`src/extension.ts` `svnWorkbench.updateScope` 与 `openProjectTask` taskMap 指向新模块；`workbenchPresentation.ts`/`terminology.ts` 标题同步；`FeatureRouter.svelte` 分发 `update` 快照。
- 批次 B 冲突直达 CTA（U-06）：`src/webview/features/update/UpdateModule.svelte`（原 repository `UpdateTask.svelte`）常驻“处理 N 个冲突”CTA（`conflicts.count > 0` 时展示数量、可展开清单、直达 conflicts 模块；采集失败如实提示且不展示 CTA）；`ChangesModule.svelte` 冲突行内“处理 X 的冲突”按钮与状态筛选区“处理 N 个冲突”按钮（open-module，范围不变）。
- 批次 C 全局推荐下一步带（C-01/C-09）：`src/extension/workbench/nextStepRecommendation.ts` 纯函数（`deriveScopeRecommendation` 优先级：冲突 > 建议提交 > AI 未配置提示 > 检查远端更新；`recommendationInputFromSnapshot` 从 changes/commit/conflicts/update 快照提取候选摘要、其他快照沿用会话缓存，不为推荐额外采集 SVN）；`WorkbenchScopeView.recommendation`（`ScopeRecommendation{key,title,reason,actionLabel,target,count?}`）随 `app/initialize`/`scope/changed` 下发，key 含状态摘要、变化才重发；`AppShell.svelte` 在 ScopeBar 下方渲染推荐带，可忽略（会话内按 key，忽略不持久惩罚），主按钮仅 open-module 不执行任何写操作。
- 批次 D Repository 分组（U-09）：`RepositoryModule.svelte` 任务导航按“分支与集成 / 维护与迁移 / 危险操作”三组（组标题 role=group 不新增标题层级）；默认展开“分支与集成”，展开状态经 `listPreferences("repository").expandedGroups` 记忆（偏好只记录用户显式展开的组）；当前任务所在组强制可见；`src/webview/app/listPreferences.ts` 扩展 `expandedGroups` 字段。
- 批次 E 文件类型筛选与预设（C-13）：`src/webview/components/list/filterPresets.ts` 共享纯逻辑（`deriveFileTypeOptions` 从候选推导后缀、`matchesFilePatterns` 简单通配符、`fileTypeToPattern`）；Changes/Commit 增加类型筛选 select 与预设行；命名预设经会话状态总线存取——`WorkbenchSession.filterPresets`（`FilterPresetView{id,name,patterns}`，仅会话内不落盘、Changes/Commit 共读）+ 动作 `list/save-filter-preset`/`list/delete-filter-preset`（同名覆盖、未知 id 忽略），快照 `ChangesSnapshot/CommitSnapshot.filterPresets` 下发；筛选与预设只影响 `filteredFiles` 视图维度，不改变选择集合与操作范围；“无扩展名”集合不支持存为预设并在 UI 如实说明。
- 批次 F 空状态矩阵（C-02）：UpdateModule（尚未生成预览=正常+检查只读+生成入口）、Changes（干净=正常+检查远端更新/查看历史快捷动作；筛选无匹配=原因+调整指引）、Commit（无候选/未选择=状态说明+回本地修改或选择推荐项）均回答“发生了什么/是否正常/现在能做什么”。
- 测试：`tests/unit/nextStepRecommendation.test.ts`（推导优先级/key 变化/快照提取/沿用缓存）、`tests/components/UpdateModule.test.ts`（令牌契约/冲突 CTA/采集失败/空态三要素/恢复入口）、`tests/components/ChangesConflictsFilter.test.ts`（冲突 CTA/类型筛选不改选择/预设保存应用删除/禁用态）、`tests/unit/workbenchProtocol.test.ts` 与 `workbenchArchitecture.test.ts`（模块守卫与标题迁移）、`tests/components/RepositoryModule.test.ts`（分组默认折叠与展开）、`tests/unit/activityExecutionRecords.test.ts`（taskId 迁移）、E2E `workbench.spec.ts`（更新独立模块+冲突 CTA+推荐带展示/忽略）、`chinese-scroll.spec.ts` SCR-07（更新独立加载与分组按需展开）、`page-screenshots.spec.ts`（09-update-preview 截图与分组展开步骤）；Mock `updateSnapshot` 工厂与 `update/preview`/`update/execute`/`list/*` 分发、scope 推荐演示。

### 8.17 深水区与打磨（v0.0.18 批次 A-E）

- 批次 A/C-03：`src/webview/app/onboarding.svelte.ts` 管理仅会话内的步骤、跳过和重开；`OnboardingStrip.svelte` 以 `role=region` 展示真实模块的下一步，`AppShell.svelte` 仅路由 `open-module`，`src/extension.ts` 的 `svnWorkbench.openGuide` 可重开。步骤不含任何执行动作，并固定在提交最终确认前结束。
- 批次 B/C-05：`src/webview/components/svn/StatusExplanation.svelte` 与 `src/webview/i18n/terminology.ts` 提供统一的键盘可达术语解释；Changes/Commit 等状态展示调用该组件，状态不以颜色作为唯一信息源。
- 批次 C/C-06：协议 `HistoryQueryView` 和 `HistorySnapshot.query` 传递实际条件；`normalizeSvnHistoryQuery` 拒绝非文本、无效 revision/日期、倒置范围和超长作者，`collectSvnHistoryPage` 仅以已校验的 revision/date 构造 `svn log --revision`，作者与日期在返回记录上只读过滤并带回 `hasMore`。`history/load-more` 条件变化时回到 100 条首批，否则递增 200；请求绑定 `AbortController`，取消保留旧快照；Controller 不再次读取而直接发送同一请求 page。Mock、协议快照、组件条件回显同步更新。
- 批次 D/E：`UnderstandingModule.svelte` 展示逐文件 seen 进度且不强制线性访问；`ScopeBar.svelte` 显示候选数、工作副本 revision、入口来源并提供可键盘操作的范围清单和复制。小高度 CSS 保证 Commit/History 至少保留一整行真实列表滚动区。
- 测试：`domainIoAdapters.test.ts` 覆盖 query 校验、受限 SVN 参数和过滤；`HistoryModule.test.ts` 覆盖五项条件 payload；`workbench.spec.ts` 覆盖 mock 条件加载；`chinese-scroll.spec.ts` SCR-03/SCR-04 覆盖 600px 下 Commit/History 末行可达。

## 9. 维护要求

1. 新增功能时同步补充“源码、测试、引入版本、证据”五项。
2. 未发布工作只链接到版本草稿，不得复用上一发布版本的通过结论。
3. 源码路径迁移后必须在同一改动中更新本表并执行 `npm run docs:verify`。
4. Workbench 新增行为应优先放入对应领域模块；Controller 只保留面板生命周期、协议路由和跨领域编排。
5. 新增绝对路径比较不得自行复制 `path.resolve + process.platform`；新增真实
   SVN fixture 不得直接清理临时目录；新增耐久写入必须提供可注入失败契约。
